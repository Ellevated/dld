# lychee link checker config
# https://lychee.cli.rs/usage/config/

# Files/dirs to skip entirely
exclude_path = [
  "ai/",
  "template/",
  "test/",
  "node_modules/",
  ".worktrees/",
]

# Per-run behavior
max_redirects = 5
no_progress = true
accept = [200, 206, 429]
