# scripts/vps/loc-limit-baseline.txt
#
# Files already over the ceiling when the gate was introduced (ARCH-209, 2026-08-30).
# The gate fails on anything NOT listed here, so this file is the debt register: a line
# is permission to stay over the limit, not permission to grow.
#
# Format: <path relative to repo root> <line count when baselined>
# A file over its limit but ABSENT here fails the gate. A file listed here that has
# since come back under the limit also fails it — stale entries hide regressions.
#
# All five are test suites. ARCH-209's scope was the production modules
# (TECH-210..216 + TECH-213 brought callback.py, orchestrator.py, db.py and
# claude-runner.py under 400); splitting the suites is separate work, because a
# test file's size is dominated by fixtures and scenario tables rather than by
# logic, and cutting one badly loses coverage rather than gaining clarity.
#
# Raising a number here is a decision, never bookkeeping — the gate is supposed to make
# the growth visible, and editing this file is how you say "seen it, took it anyway".
# Log every raise below, or the register stops meaning anything.
#
# 2026-09-06  test_db.py 761 → 780, test_orchestrator.py 1703 → 1706 (19d27f1e, third
#             claude slot). Both grew by covering the new slot count — the +19 lines in
#             test_db are the CLAUDE_SLOTS/CLAUDE_SLOT_NUMBERS constants plus the
#             schema.sql cross-check that stops the next capacity change from being a
#             hunt for red asserts. CI had been red on this since the push.

# 2026-09-07  orchestrator.py enters the register at 412 (was 400, exactly at the
#             ceiling). Two changes, both about the queue no longer stopping: the
#             dispatch loop that skips a defective spec instead of the whole project
#             (dowry BUG-507 held five ready specs), and DISPATCH_MODE, which hands the
#             decision to the dispatcher skill and keeps `builtin` as a one-env-var
#             rollback. Splitting the loop out is not free — 13 tests monkeypatch
#             `orchestrator.<bare name>`, which only resolves in this module's globals.
#             Taken deliberately; the next line here should be a REDUCTION.
#             test_orchestrator.py 1706 → 1785: two tests for the new loop (a refused
#             spec must not stop the project) plus the per-file ceiling map.

scripts/vps/orchestrator.py 412
scripts/vps/tests/test_callback.py 813
scripts/vps/tests/test_db.py 780
scripts/vps/tests/test_lifecycle.py 827
scripts/vps/tests/test_orchestrator.py 1785
scripts/vps/tests/test_orchestrator_bootstrap.py 692
