# BUG-218 — advisory findings

| File:line | Finding | Suggested action |
|-----------|---------|------------------|
| `scripts/vps/tests/test_orchestrator_in_progress.py` | Девятая приватная копия фикстуры `tmp_git_repo` (не третья, как предполагала спека) | Хойст в `conftest.py` — правка девяти файлов, отдельная TECH-спека |
| `scripts/vps/orchestrator.py:589` | Fail-closed пропуск одноразовый: `startup_reconcile` не повторяется в poll-цикле. Pueue лёг на старте и поднялся без рестарта → осиротевшие `in_progress` не демоутятся никогда | Либо флаг «reconcile pending» с ретраем в цикле, либо зафиксировать рестарт как санкционированный recovery в `runbook.md` |
| `scripts/vps/orchestrator.py` | 1117 LOC против потолка 400 | TECH-215 (раскол), уже в бэклоге с зависимостью AFTER BUG-218 |
| `scripts/vps/tests/test_orchestrator.py` | 1335 LOC против потолка 600 для тестов | Раскол по классам гейтов — отдельная TECH-спека |
| `scripts/vps/tests/test_orchestrator.py:1243` | `TestProviderSelection._dispatch` делят 4 теста, один из них `result is False` — патч там инертен | Ничего: неизбежно для общего хелпера, инертный патч ничего не стоит |
| `lifecycle.py:577` `_push_best_effort` | Частота lifecycle-push удваивается: теперь коммит на каждый диспатч, а не только на завершение | Наблюдать счётчик `ai/.lifecycle-push-failures` после деплоя |
