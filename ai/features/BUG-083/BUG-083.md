# Bug Hunt: [BUG-083] DLD Framework Full Audit

**Status:** done | **Priority:** P0 | **Date:** 2026-02-14
**Mode:** Bug Hunt (multi-agent)
**Cost:** ~$80 (6 Sonnet personas + 2 Opus frameworks + 1 Opus validator)

## Original Problem

Full project audit: find all bugs in DLD framework (template, hooks, scripts, docs, npm package).

## Executive Summary

- Total raw findings: 147
- Phase 1a (6 personas): 147 findings
- Phase 1b (TOC + TRIZ): 8 UDEs, 7 technical contradictions, 3 root causes
- Relevant (in scope): 41 (8 CRITICAL, 16 HIGH, 13 MEDIUM, 4 LOW)
- Out of scope (-> ideas.md): 96
- Duplicates merged: 10
- Sub-specs created: 6

## Phase 1a: Persona Findings

### Code Reviewer (CR) - 8 findings

| ID | Severity | Title |
|----|----------|-------|
| CR-001 | CRITICAL | Missing ci-status.sh referenced by autopilot worktree-setup |
| CR-002 | CRITICAL | Missing pre-review-check.py in template/scripts/ |
| CR-003 | HIGH | Tier numbering mismatch (create-dld 0/1/2 vs setup-mcp.sh 1/2/3) |
| CR-004 | MEDIUM | Missing .gitignore in template |
| CR-005 | LOW | Brandbook skill in root but not template |
| CR-006 | LOW | install-hooks.sh references wrong directory |
| CR-007 | LOW | CLAUDE.md tier 3 vs create-dld tier 2 mismatch |
| CR-008 | LOW | installation-guide.md tier references inconsistent |

### Security Auditor (SEC) - 18 findings

| ID | Severity | Title |
|----|----------|-------|
| SEC-001 | CRITICAL | defaultMode=bypassPermissions in settings.json |
| SEC-002 | CRITICAL | Wildcard mcp__* grants access to ALL MCP servers |
| SEC-003 | HIGH | Path traversal in hook loading via hookName |
| SEC-004 | HIGH | Arbitrary file read via malicious spec path |
| SEC-005 | HIGH | Regex bypass with shell metacharacters in pre-bash |
| SEC-006 | HIGH | TOCTOU race condition in create-dld |
| SEC-007 | HIGH | Command injection potential via REPO_URL |
| SEC-008 | MEDIUM | eval() with unsanitized command strings in setup-mcp.sh |
| SEC-009 | MEDIUM | Path traversal via CLAUDE_PROJECT_DIR |
| SEC-010 | MEDIUM | Log file injection |
| SEC-011 | MEDIUM | Command injection via malicious filename in ruff |
| SEC-012 | MEDIUM | Arbitrary command execution via git diff |
| SEC-013 | MEDIUM | Backlog file injection via grep |
| SEC-014 | LOW | Arbitrary file copy via glob expansion |
| SEC-015 | LOW | Command injection via git worktree list |
| SEC-016 | LOW | Regex injection in minimatch |
| SEC-017 | CRITICAL | Hardcoded API key placeholder in template |
| SEC-018 | LOW | Inconsistent force-with-lease check |

### UX Analyst (UX) - 35 findings

| ID | Severity | Title |
|----|----------|-------|
| UX-001 | HIGH | Template references non-existent test script |
| UX-002 | CRITICAL | Broken documentation links in getting started table |
| UX-003 | MEDIUM | Installation instruction assumes existing project |
| UX-004 | MEDIUM | MCP setup script referenced without explanation |
| UX-005 | LOW | Vague next steps after MCP setup |
| UX-006 | MEDIUM | Installation protocol backup to non-existent directory |
| UX-007 | HIGH | Installation steps assume /tmp/dld-install exists |
| UX-008 | CRITICAL | Contradictory MCP configuration instructions |
| UX-009 | HIGH | Bootstrap doc references non-existent ai/principles/ |
| UX-010 | MEDIUM | Minimal set instructions outdated |
| UX-011 | LOW | Version number mismatch in header |
| UX-012 | MEDIUM | Package README doesn't explain /bootstrap |
| UX-013 | HIGH | CONTRIBUTING.md references check-sync.sh |
| UX-014 | MEDIUM | References non-existent CUSTOMIZATIONS.md |
| UX-015 | LOW | Outdated quick start path in FAQ |
| UX-016 | CRITICAL | MCP command line is unreadable (200+ chars) |
| UX-017 | MEDIUM | Incorrect Claude Code installation command |
| UX-018 | LOW | Quick start example too complex |
| UX-019 | MEDIUM | Template asks user to fill multiple placeholders |
| UX-020 | HIGH | Installation preview table too complex for LLM |
| UX-021 | HIGH | ID protocol example contradicts sequential numbering |
| UX-022 | LOW | Project structure shows council agents moved |
| UX-023 | HIGH | Memory MCP setup instructions unclear |
| UX-024 | MEDIUM | Outdated model reference (Opus 4.5 vs 4.6) |
| UX-025 | LOW | Skills list in README doesn't match CLAUDE.md |
| UX-026 | MEDIUM | Memory MCP setup missing actual instructions |
| UX-027 | CRITICAL | Template missing setup-mcp.sh script |
| UX-028 | HIGH | 'Used By' section lists unverified projects |
| UX-029 | MEDIUM | Upgrade instructions reference manual editing |
| UX-030 | LOW | Learning time estimates optimistic |
| UX-031 | HIGH | Git autonomous mode unexplained |
| UX-032 | MEDIUM | Onboarding references import linter without setup |
| UX-033 | LOW | Python syntax in language-agnostic docs |
| UX-034 | MEDIUM | Python-specific commands in Development section |
| UX-035 | LOW | Combine them section unclear |

### Junior Developer (JR) - 36 findings

| ID | Severity | Title |
|----|----------|-------|
| JR-001 | CRITICAL | No README.md in template directory |
| JR-002 | CRITICAL | No .gitignore in template |
| JR-003 | HIGH | Installation section contradicts itself |
| JR-004 | HIGH | Python appears randomly with no context |
| JR-005 | HIGH | No clear reading order for docs |
| JR-006 | HIGH | Quick start assumes knowledge of hooks/MCP |
| JR-007 | MEDIUM | Bootstrap doc references non-existent ai/principles/ |
| JR-008 | MEDIUM | Create-dld success message doesn't explain /bootstrap |
| JR-009 | MEDIUM | Project structure in README doesn't match template |
| JR-010 | MEDIUM | Standard tier doesn't include advertised features |
| JR-011 | MEDIUM | Template CLAUDE.md starts with unexplained placeholders |
| JR-012 | LOW | setup-mcp.sh has --help but others don't |
| JR-013 | LOW | Examples are very advanced, no simple example |
| JR-014 | LOW | Project structure shows dirs not in template |
| JR-015 | MEDIUM | MCP setup: CLI commands vs .mcp.json.example unclear |
| JR-016 | HIGH | /bootstrap not positioned as mandatory Day 0 |
| JR-017 | MEDIUM | Contributing guide says 'run tests' but doesn't explain how |
| JR-018 | LOW | Why doc is philosophical, lacks concrete pain points |
| JR-019 | CRITICAL | Backlog template empty with no examples |
| JR-020 | HIGH | MCP appears AFTER installation but is part of setup |
| JR-021 | MEDIUM | Spec template unclear if manual or auto-generated |
| JR-022 | LOW | Numbered docs don't have an index |
| JR-023 | MEDIUM | Agents directory has no README |
| JR-024 | HIGH | README says 'bootstrap will guide' but no preview |
| JR-025 | MEDIUM | Onboarding doc conflicts with /bootstrap workflow |
| JR-026 | LOW | FAQ not linked from README |
| JR-027 | MEDIUM | COMPARISON.md not linked from README |
| JR-028 | CRITICAL | No troubleshooting guide |
| JR-029 | MEDIUM | Template includes installation-guide.md (confusing for users) |
| JR-030 | HIGH | No skills/README.md explaining when to use which |
| JR-031 | LOW | Examples have no TL;DR |
| JR-032 | MEDIUM | Getting Started paths lack 'choose this if...' guidance |
| JR-033 | LOW | Naming doc too thin for standalone file |
| JR-034 | HIGH | Skills setup doc version confusion (v3.4 vs v3.7) |
| JR-035 | MEDIUM | Template scripts lack README |
| JR-036 | CRITICAL | README assumes user already in Claude session |

### QA Engineer (QA) - 33 findings

| ID | Severity | Title |
|----|----------|-------|
| QA-001 | CRITICAL | Missing 'prompts' dependency in local dev |
| QA-002 | HIGH | Race condition in create-dld directory check |
| QA-003 | HIGH | Temp directory cleanup failure |
| QA-004 | MEDIUM | Error detection misses specific git errors |
| QA-005 | MEDIUM | Git init fails silently if git unavailable post-check |
| QA-006 | MEDIUM | MCP setup has no rollback on partial failure |
| QA-007 | LOW | Project name validation allows '..' |
| QA-008 | HIGH | inferSpecFromBranch fails on detached HEAD (bypass) |
| QA-009 | MEDIUM | Dry-run detection regex misses flag orders |
| QA-010 | MEDIUM | minimatch doesn't support ** (globstar) |
| QA-011 | LOW | normalizePath uses env with no validation |
| QA-012 | HIGH | smoke-test.sh uses node which may not exist |
| QA-013 | MEDIUM | REQUIRED_HOOKS list doesn't match actual hooks |
| QA-014 | MEDIUM | autopilot command output capture loses stderr |
| QA-015 | LOW | --check mode doesn't validate backlog file exists |
| QA-016 | CRITICAL | install-hooks.sh no validation that .git exists |
| QA-017 | MEDIUM | Overwrites existing hooks without backup |
| QA-018 | MEDIUM | check-sync.sh hardcoded extensions miss .mjs |
| QA-019 | LOW | Line count diff doesn't account for whitespace changes |
| QA-020 | HIGH | Not all hooks in template registered in settings.json |
| QA-021 | MEDIUM | Session end hook outputs empty JSON |
| QA-022 | LOW | ruff format timeout doesn't report filename |
| QA-023 | CRITICAL | Template missing .gitignore |
| QA-024 | MEDIUM | Sparse checkout may fail on old git versions |
| QA-025 | LOW | Hook checks 'git commit' substring, not command |
| QA-026 | MEDIUM | Python version not enforced |
| QA-027 | LOW | Coverage source points to .mjs files |
| QA-028 | HIGH | eval executes untrusted commands in setup-mcp.sh |
| QA-029 | MEDIUM | Health check doesn't validate Context7 output |
| QA-030 | LOW | Force-with-lease check code structure confusing |
| QA-031 | CRITICAL | Regex for extracting allowed files misses formats |
| QA-032 | MEDIUM | Template scripts missing from scripts/ |
| QA-033 | LOW | Hook not found message contradictory |

### Software Architect (ARCH) - 17 findings

| ID | Severity | Title |
|----|----------|-------|
| ARCH-001 | CRITICAL | scaffold skill documented but doesn't exist |
| ARCH-002 | CRITICAL | settings.local.json not gitignored |
| ARCH-003 | HIGH | defaultMode in root but not template (sync violation) |
| ARCH-004 | HIGH | localization.md differs significantly (Russian vs Spanish) |
| ARCH-005 | HIGH | ai/glossary/ referenced by 7 files but missing |
| ARCH-006 | HIGH | ai/ARCHITECTURE.md referenced but missing |
| ARCH-007 | MEDIUM | check-sync.sh doesn't check .mjs files |
| ARCH-008 | MEDIUM | Root has 3 extra scripts not in template |
| ARCH-009 | MEDIUM | Bug-hunt agents not tracked in CUSTOMIZATIONS.md |
| ARCH-010 | MEDIUM | skill-writer SKILL.md minor template drift |
| ARCH-011 | MEDIUM | git-local-folders.md not flagged as DLD-specific |
| ARCH-012 | LOW | template-sync.md not in template |
| ARCH-013 | LOW | Hooks perfectly synced (positive finding) |
| ARCH-014 | LOW | Bug-hunt agents in both but not documented |
| ARCH-015 | LOW | create-dld package.json files excludes template |
| ARCH-016 | LOW | .gitignore blocks entire .claude/ directory |
| ARCH-017 | CRITICAL | Root settings.json violates .gitignore intent |

## Phase 1b: Framework Analysis

### TOC (Theory of Constraints)

**8 UDEs identified:**
1. Phantom References (31 findings) - docs reference non-existent files
2. Template/Root Desynchronization (18 findings) - two copies drift apart
3. Insecure Defaults (8 findings) - bypassPermissions, mcp__*, unsanitized inputs
4. Broken Onboarding (22 findings) - no README, no .gitignore, contradictions
5. Hook Logic Gaps (12 findings) - regex misses, detached HEAD bypass
6. Contradictory Install Paths (10 findings) - tier numbering mismatch
7. Incomplete Template Infrastructure (15 findings) - 4 of 6 scripts missing
8. No Automated Validation (8 findings) - deleted tests never replaced

**Core Constraint:** No validation layer exists between "written" and "shipped."

**5 Injections (covering 91/147 = 62%):**
1. Doc-link validator (31+ findings, LOW effort)
2. Secure template defaults (8 CRITICAL findings, LOW effort)
3. Template-root sync CI check (18+ findings, LOW effort)
4. Hook test suite (12+ findings, MEDIUM effort)
5. Golden path onboarding (22+ findings, MEDIUM effort)

### TRIZ

**7 Technical Contradictions:**
1. Security vs Usability
2. Template Completeness vs Simplicity
3. Dual-Copy Architecture vs Sync Consistency
4. Documentation Richness vs Accuracy
5. Hook Safety vs Resilience
6. Multi-Agent Power vs Onboarding Simplicity
7. Cross-Platform Portability vs Tool-Chain Depth

**4 Physical Contradictions:**
1. Template must be FULL and EMPTY simultaneously
2. Settings must be SECURE and PERMISSIVE simultaneously
3. Documentation must be COMPREHENSIVE and CONCISE simultaneously
4. System must be AUTONOMOUS and SUPERVISED simultaneously

**Ideality:** 1.14 (target >3.0) - system barely delivers more value than problems

**3 Root Causes generating ~80% of findings:**
1. Dual-copy architecture (template/ vs root .claude/)
2. Fail-open hook design (ADR-004)
3. Organic documentation growth without validation

## Consensus Matrix

| Finding Cluster | CR | SEC | UX | JR | QA | ARCH | TOC | TRIZ | Total |
|----------------|-----|-----|-----|-----|-----|------|-----|------|-------|
| Missing template files | x | | x | x | x | x | x | x | 7/8 |
| Security defaults | | x | | | | x | x | x | 4/8 |
| Broken doc references | x | | x | x | | x | x | x | 6/8 |
| Tier numbering | x | | x | x | | | x | x | 5/8 |
| Hook logic gaps | | x | | | x | | x | x | 4/8 |
| Onboarding cliff | | | x | x | | | x | x | 4/8 |
| Template-root sync | x | x | | | x | x | x | x | 6/8 |
| No test suite | | | | | x | | x | x | 3/8 |

## Phase 2: Validation Results

- **41 RELEVANT** (actionable bugs): 8 CRITICAL, 16 HIGH, 13 MEDIUM, 4 LOW
- **96 OUT OF SCOPE** (false positives, enhancements, already known)
- **10 duplicate pairs** collapsed

Key validator corrections:
- SEC-001 (bypassPermissions) → OUT OF SCOPE (root .claude/ is gitignored, never ships to users)
- ARCH-002 (settings.local.json) → FALSE POSITIVE (documented in CUSTOMIZATIONS.md)
- SEC-017 (API key placeholder) → FALSE POSITIVE (in .example file)

## Execution Plan

| Sub-spec | Title | Priority | Effort | Status |
|----------|-------|----------|--------|--------|
| [BUG-083-01](BUG-083-01.md) | Template Missing Files | CRITICAL | ~2h | queued |
| [BUG-083-02](BUG-083-02.md) | Security Defaults | CRITICAL | ~2h | queued |
| [BUG-083-03](BUG-083-03.md) | Tier Numbering Alignment | HIGH | ~2h | queued |
| [BUG-083-04](BUG-083-04.md) | Hook Logic & Testing | HIGH | ~4-6h | queued |
| [BUG-083-05](BUG-083-05.md) | Documentation Accuracy | HIGH | ~1-2h | queued |
| [BUG-083-06](BUG-083-06.md) | Smoke Test & Validation | MEDIUM | ~2h | queued |

**Total estimated effort:** 13-16 hours
**Execution order:** 01 → 02 → 03 → 04 → 05 → 06 (CRITICAL first, then HIGH, then MEDIUM)

### 3 Root Causes (from TRIZ)

1. **Dual-copy architecture** (template/ vs root .claude/) → BUG-083-01, 03, 06
2. **Fail-open hook design** (ADR-004 edge cases) → BUG-083-04
3. **Organic documentation growth** without validation → BUG-083-05

### TOC Core Constraint

**No validation layer exists between "written" and "shipped."**
BUG-083-06 (smoke test) partially addresses this.

---

## Drift Log

**Checked:** 2026-02-14 UTC
**Result:** light_drift

### Changes Detected
| File | Change Type | Action Taken |
|------|-------------|--------------|
| `template/ai/glossary/_template.md` | already_exists | AUTO-FIX: BUG-083-05 task updated, skip creation |
| BUG-083-03 scope | missing_files_in_spec | AUTO-FIX: noted `docs/getting-started/*.md`, `template/ai/installation-guide.md` have `--tier N` refs but outside allowed files -- kept as-is per spec scope |

### References Updated
- BUG-083-05 Task: `Create ai/glossary/_template.md` -> `SKIP (already exists in template/ai/glossary/_template.md)`
- BUG-083-05 Task: `Create ai/ARCHITECTURE.md` -> `Create template/ai/ARCHITECTURE.md` (template dir, not root ai/)

---

## Detailed Implementation Plan

**Total Tasks:** 8
**Execution Order:** Task 1 -> Task 2 -> Task 3 -> Task 4 -> Task 5 -> Task 6 -> Task 7 -> Task 8
**All file paths are relative to worktree root:** `.worktrees/BUG-083/`

---

### Task 1: Create template/README.md and template/.gitignore [BUG-083-01]

**Files:**
- Create: `template/README.md`
- Create: `template/.gitignore`
- Create: `template/scripts/ci-status.sh`

**Context:**
Template directory is missing three critical files that users expect on Day 0 after `npx create-dld`. README.md provides first orientation, .gitignore prevents ai/ from being tracked, ci-status.sh is referenced by autopilot worktree-setup.

**Step 1: Create template/README.md**

Create file `template/README.md` with content:

```markdown
# {Project Name}

Created with [DLD Framework](https://github.com/Ellevated/dld).

## Getting Started

1. Run `claude` to start Claude Code
2. Say `/bootstrap` to unpack your idea
3. Say `/spark` to create your first feature

## Commands

- `./scripts/setup-mcp.sh` -- Configure MCP servers
- `./scripts/smoke-test.sh` -- Validate project structure

## Learn More

- [DLD Documentation](https://github.com/Ellevated/dld/tree/main/docs)
- [Quick Start Guide](https://github.com/Ellevated/dld/blob/main/docs/getting-started/quick-start.md)
```

**Step 2: Create template/.gitignore**

Create file `template/.gitignore` with content:

```gitignore
# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# DLD local artifacts
/ai/
```

**Step 3: Create template/scripts/ci-status.sh**

Create file `template/scripts/ci-status.sh` with content:

```bash
#!/bin/bash
# CI status check -- stub for local development
# Override this with your actual CI check command
echo "CI status: OK (stub -- configure for your CI)"
exit 0
```

Then make it executable:
```bash
chmod +x template/scripts/ci-status.sh
```

**Acceptance Criteria:**
- [ ] `template/README.md` exists and contains getting started instructions
- [ ] `template/.gitignore` exists and includes `/ai/` entry
- [ ] `template/scripts/ci-status.sh` exists and is executable
- [ ] All three files are under 50 lines

---

### Task 2: Fix template/CLAUDE.md (doc links + tier naming + version) [BUG-083-01, 03, 05]

**Files:**
- Modify: `template/CLAUDE.md`

**Context:**
Three sub-specs modify template/CLAUDE.md. This task combines all changes: (1) replace broken relative doc links with GitHub URLs, (2) fix tier numbering from `--tier 3` to `--power`, (3) update version from v3.4 to v3.7. These are all text/documentation changes to a single file.

**Step 1: Fix tier reference on line 23**

Replace:
```
**Upgrade:** Run `./scripts/setup-mcp.sh --tier 3` for Power tier (council, autopilot, planner)
```
With:
```
**Upgrade:** Run `./scripts/setup-mcp.sh --power` for Power tier (council, autopilot, planner)
```

**Step 2: Fix version on line 69**

Replace:
```
## Project Context System (v3.4)
```
With:
```
## Project Context System (v3.7)
```

**Step 3: Fix version on line 122**

Replace:
```
## Skills (v3.4)
```
With:
```
## Skills (v3.7)
```

**Step 4: Fix version on line 168**

Replace:
```
**New in v3.4:**
```
With:
```
**New in v3.7:**
```

**Step 5: Fix version on line 260**

Replace:
```
├── diary/      # Session learnings (v3.4)
```
With:
```
├── diary/      # Session learnings (v3.7)
```

**Step 6: Fix the `ai/ARCHITECTURE.md` reference on line 55**

No change needed -- `ai/ARCHITECTURE.md` is created by bootstrap, the reference is correct. The file will be created in Task 7.

**Verification:**
```bash
grep "v3\.4" template/CLAUDE.md
# Expected: 0 results

grep "\-\-tier 3" template/CLAUDE.md
# Expected: 0 results
```

**Acceptance Criteria:**
- [ ] `grep "v3\.4" template/CLAUDE.md` returns 0 results
- [ ] `grep "\-\-tier 3" template/CLAUDE.md` returns 0 results
- [ ] All 4 version references updated to v3.7
- [ ] Tier reference uses `--power` flag name

---

### Task 3: Security defaults -- MCP whitelist and path traversal protection [BUG-083-02]

**Files:**
- Modify: `template/.claude/settings.json`
- Modify: `template/.claude/hooks/utils.mjs`
- Modify: `template/.claude/hooks/run-hook.mjs`

**Context:**
Template ships with `mcp__*` wildcard that grants access to ALL MCP servers, including any malicious ones a user might accidentally install. Hook loading has no validation on hookName, allowing path traversal. CLAUDE_PROJECT_DIR is used without validation. All fixes go to template first (universal improvement per sync rule).

**Step 1: Replace mcp__* wildcard in template settings.json**

In file `template/.claude/settings.json`, replace:
```json
      "mcp__*"
```
With:
```json
      "mcp__plugin_context7_context7__resolve-library-id",
      "mcp__plugin_context7_context7__query-docs",
      "mcp__exa__web_search_exa",
      "mcp__exa__web_search_advanced_exa",
      "mcp__exa__get_code_context_exa",
      "mcp__exa__deep_search_exa",
      "mcp__exa__crawling_exa",
      "mcp__exa__company_research_exa",
      "mcp__exa__deep_researcher_start",
      "mcp__exa__deep_researcher_check"
```

**Step 2: Add getProjectDir() to template utils.mjs**

In file `template/.claude/hooks/utils.mjs`, after the line:
```javascript
import { execFileSync } from 'child_process';
```

Add:
```javascript
import { resolve } from 'path';
```

Wait -- `resolve` is not imported but `normalize` is already imported from `path`. Let me check... Actually line 15 already imports `join, dirname, normalize` from path. We need to also import `resolve`. Let me update the import.

In file `template/.claude/hooks/utils.mjs`, replace:
```javascript
import { join, dirname, normalize } from 'path';
```
With:
```javascript
import { join, dirname, normalize, resolve } from 'path';
```

Then, after the `getUserPrompt` function (after line 121), add:
```javascript

// --- Path safety ---

/**
 * Get project directory with path traversal protection.
 * Falls back to cwd() if CLAUDE_PROJECT_DIR escapes home or /tmp.
 */
export function getProjectDir() {
  const dir = process.env.CLAUDE_PROJECT_DIR || process.cwd();
  const resolved = resolve(dir);
  const home = homedir();
  if (!resolved.startsWith(home) && !resolved.startsWith('/tmp')) {
    return process.cwd();
  }
  return resolved;
}
```

**Step 3: Add hookName validation to template run-hook.mjs**

In file `template/.claude/hooks/run-hook.mjs`, replace:
```javascript
const hookName = process.argv[2];
if (!hookName) {
  process.exit(0); // No hook specified — silently allow
}
```
With:
```javascript
const hookName = process.argv[2];
if (!hookName) {
  process.exit(0); // No hook specified — silently allow
}

// Validate hookName — alphanumeric, hyphens, dots only (prevent path traversal)
if (!/^[a-zA-Z0-9._-]+$/.test(hookName)) {
  process.exit(0); // Silent fail-safe for invalid hook names
}
```

**Step 4: Sync changes to root hooks**

Copy template hooks to root:
```bash
cp template/.claude/hooks/utils.mjs .claude/hooks/utils.mjs
cp template/.claude/hooks/run-hook.mjs .claude/hooks/run-hook.mjs
```

NOTE: Do NOT copy yet -- Task 4 makes additional changes to these same files. The sync happens at end of Task 4.

**Acceptance Criteria:**
- [ ] `template/.claude/settings.json` has no `mcp__*` wildcard
- [ ] `template/.claude/settings.json` has 10 explicit MCP tool permissions
- [ ] `template/.claude/hooks/utils.mjs` exports `getProjectDir()`
- [ ] `template/.claude/hooks/run-hook.mjs` validates hookName with regex
- [ ] JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('template/.claude/settings.json','utf-8'))"`

---

### Task 4: Hook logic fixes -- minimatch, inferSpec, extractAllowed, error msg, force-with-lease [BUG-083-04]

**Files:**
- Modify: `template/.claude/hooks/utils.mjs`
- Modify: `template/.claude/hooks/run-hook.mjs`
- Modify: `template/.claude/hooks/pre-bash.mjs`

**Context:**
Seven hook logic issues need fixing. The minimatch function doesn't support `**` globstar. inferSpecFromBranch silently fails on detached HEAD. extractAllowedFiles misses paths with dashes. run-hook.mjs prints contradictory "Allowed: hook not found" message. pre-bash.mjs has redundant force-with-lease check. All changes go to template first (universal), then sync to root.

**Step 1: Fix minimatch to support ** globstar**

In file `template/.claude/hooks/utils.mjs`, replace the entire minimatch function (lines 147-155):
```javascript
function minimatch(str, pattern) {
  // Escape regex specials, then convert glob to regex
  let re = pattern
    .replace(/[.+^${}()|[\]\\]/g, '\\$&')
    .replace(/\*/g, '[^/]*')
    .replace(/\?/g, '[^/]');
  re = `^${re}$`;
  return new RegExp(re).test(str);
}
```
With:
```javascript
function minimatch(str, pattern) {
  // Escape regex specials, then convert glob to regex
  // Use placeholder for ** before converting single *
  let re = pattern
    .replace(/[.+^${}()|[\]\\]/g, '\\$&')
    .replace(/\*\*/g, '\x00GLOBSTAR\x00')
    .replace(/\*/g, '[^/]*')
    .replace(/\?/g, '[^/]')
    .replace(/\x00GLOBSTAR\x00/g, '.*');
  re = `^${re}$`;
  return new RegExp(re).test(str);
}
```

**Step 2: Fix inferSpecFromBranch for detached HEAD**

In file `template/.claude/hooks/utils.mjs`, replace the entire `inferSpecFromBranch` function (lines 186-214):
```javascript
export function inferSpecFromBranch() {
  try {
    const branch = execFileSync('git', ['branch', '--show-current'], {
      timeout: GIT_TIMEOUT_MS,
      encoding: 'utf-8',
    }).trim();

    if (!branch) return null;

    const match = branch.match(/(FTR|BUG|TECH|ARCH|SEC)-\d+/i);
    if (!match) return null;

    const taskId = match[0].toUpperCase();
    const pattern = `ai/features/${taskId}-`;

    // Simple glob: list files in ai/features/ matching prefix
    try {
      const dir = 'ai/features';
      if (!existsSync(dir)) return null;
      const files = readdirSync(dir);
      const found = files.find(f => f.startsWith(`${taskId}-`) && f.endsWith('.md'));
      return found ? join(dir, found) : null;
    } catch {
      return null;
    }
  } catch {
    return null; // fail-safe: git error = no spec inference
  }
}
```
With:
```javascript
export function inferSpecFromBranch() {
  try {
    // 1. Try branch name first
    let branch = execFileSync('git', ['branch', '--show-current'], {
      timeout: GIT_TIMEOUT_MS,
      encoding: 'utf-8',
    }).trim();

    // 2. Fallback: detached HEAD — try symbolic-ref
    if (!branch) {
      try {
        branch = execFileSync('git', ['symbolic-ref', '--short', 'HEAD'], {
          timeout: GIT_TIMEOUT_MS,
          encoding: 'utf-8',
        }).trim();
      } catch {
        // 3. Last resort: extract task ID from latest commit message
        try {
          const commitMsg = execFileSync('git', ['log', '-1', '--pretty=%s'], {
            timeout: GIT_TIMEOUT_MS,
            encoding: 'utf-8',
          }).trim();
          const msgMatch = commitMsg.match(/(FTR|BUG|TECH|ARCH|SEC)-\d+/i);
          if (msgMatch) branch = msgMatch[0];
        } catch {
          return null; // fail-safe (ADR-004)
        }
      }
    }

    if (!branch) return null;

    const match = branch.match(/(FTR|BUG|TECH|ARCH|SEC)-\d+/i);
    if (!match) return null;

    const taskId = match[0].toUpperCase();

    // Look for spec file in ai/features/ matching prefix
    try {
      const dir = 'ai/features';
      if (!existsSync(dir)) return null;
      const files = readdirSync(dir);
      const found = files.find(f => f.startsWith(`${taskId}-`) && f.endsWith('.md'));
      return found ? join(dir, found) : null;
    } catch {
      return null;
    }
  } catch {
    return null; // fail-safe: git error = no spec inference
  }
}
```

**Step 3: Fix extractAllowedFiles regex for paths with dashes**

In file `template/.claude/hooks/utils.mjs`, replace line 171:
```javascript
      const pathMatch = trimmed.match(/[`*]*([a-zA-Z0-9_./-]+\.[a-zA-Z0-9]+(?::\d+(?:-\d+)?)?)[`*]*/);
```
With:
```javascript
      const pathMatch = trimmed.match(/[`*\-]*\s*([a-zA-Z0-9_./@-]+\.[a-zA-Z0-9]+(?::\d+(?:-\d+)?)?)[`*]*/);
```

**Step 4: Fix run-hook.mjs contradictory message**

In file `template/.claude/hooks/run-hook.mjs`, replace:
```javascript
  // Hook not found — silently allow
  process.stderr.write(`\u2713 Allowed: hook ${hookName} not found\n`);
  process.exit(0);
```
With:
```javascript
  // Hook not found — silently allow (ADR-004: fail-safe)
  process.exit(0);
```

**Step 5: Fix pre-bash.mjs redundant force-with-lease check**

In file `template/.claude/hooks/pre-bash.mjs`, replace:
```javascript
    // Hard blocks (deny immediately)
    for (const [pattern, message] of BLOCKED_PATTERNS) {
      if (pattern.test(command)) {
        // Allow --force-with-lease (safe force push)
        if (command.includes('--force-with-lease')) continue;
        denyTool(message);
        return;
      }
    }
```
With:
```javascript
    // Hard blocks (deny immediately)
    // Note: --force-with-lease is handled by regex negative lookahead (?!-with-lease)
    for (const [pattern, message] of BLOCKED_PATTERNS) {
      if (pattern.test(command)) {
        denyTool(message);
        return;
      }
    }
```

**Step 6: Sync ALL hook changes to root .claude/hooks/**

After all template hook changes are complete:
```bash
cp template/.claude/hooks/utils.mjs .claude/hooks/utils.mjs
cp template/.claude/hooks/run-hook.mjs .claude/hooks/run-hook.mjs
cp template/.claude/hooks/pre-bash.mjs .claude/hooks/pre-bash.mjs
```

**Verification:**
```bash
diff template/.claude/hooks/utils.mjs .claude/hooks/utils.mjs
# Expected: no output (identical)

diff template/.claude/hooks/run-hook.mjs .claude/hooks/run-hook.mjs
# Expected: no output (identical)

diff template/.claude/hooks/pre-bash.mjs .claude/hooks/pre-bash.mjs
# Expected: no output (identical)
```

**Acceptance Criteria:**
- [ ] minimatch handles `**` patterns: `minimatch('ai/diary/2026/jan.md', 'ai/diary/**/*.md')` returns true
- [ ] inferSpecFromBranch has 3-level fallback (branch -> symbolic-ref -> commit message)
- [ ] extractAllowedFiles handles dashed paths like `template/.claude/hooks/pre-bash.mjs`
- [ ] run-hook.mjs is silent when hook not found (no stderr output)
- [ ] pre-bash.mjs has no redundant `command.includes('--force-with-lease')` check
- [ ] Root and template hooks are identical (`diff` returns empty)

---

### Task 5: Fix setup-mcp.sh tier labels and create-dld tier references [BUG-083-03]

**Files:**
- Modify: `scripts/setup-mcp.sh`
- Modify: `template/scripts/setup-mcp.sh`
- Modify: `packages/create-dld/index.js`

**Context:**
Tier numbering is inconsistent: create-dld uses 0/1/2 internally, setup-mcp.sh uses 1/2/3 with "Zero/Recommended/Power" labels. The canonical naming is Quick/Standard/Power with `--quick/--standard/--power` flags. The setup-mcp.sh script keeps numeric tiers internally (1/2/3) for backward compatibility but display labels change. create-dld messages referencing "Tier 3" need updating.

**Step 1: Update setup-mcp.sh tier display labels**

In file `scripts/setup-mcp.sh`, replace:
```bash
        1)
            echo -e "\n${GREEN}Tier 1: Zero${NC}"
            echo "No MCP servers to configure. DLD will use built-in tools."
            ;;
        2)
            echo -e "\n${GREEN}Tier 2: Recommended (Context7 + Exa)${NC}"
```
With:
```bash
        1)
            echo -e "\n${GREEN}Quick: No MCP${NC}"
            echo "No MCP servers to configure. DLD will use built-in tools."
            ;;
        2)
            echo -e "\n${GREEN}Standard: Context7 + Exa${NC}"
```

And replace:
```bash
        3)
            echo -e "\n${GREEN}Tier 3: Power${NC}"
            # First add Tier 2
            setup_tier 2
```
With:
```bash
        3)
            echo -e "\n${GREEN}Power: All MCP Servers${NC}"
            # First add Standard tier servers
            setup_tier 2
```

And replace the interactive menu labels:
```bash
    echo "  1) Zero      - No MCP (quick evaluation)"
    echo "  2) Recommended - Context7 + Exa (no API keys)"
    echo "  3) Power     - + Memory + Sequential (requires keys)"
```
With:
```bash
    echo "  1) Quick      - No MCP (quick evaluation)"
    echo "  2) Standard   - Context7 + Exa (no API keys) [recommended]"
    echo "  3) Power      - + Memory + Sequential (requires keys)"
```

And update the help text:
```bash
            echo "  --tier N      Non-interactive setup (1=Zero, 2=Recommended, 3=Power)"
```
With:
```bash
            echo "  --tier N      Non-interactive setup (1=Quick, 2=Standard, 3=Power)"
```

And update the usage line in the comment:
```bash
#   ./scripts/setup-mcp.sh --tier 2 # Non-interactive setup
```
With:
```bash
#   ./scripts/setup-mcp.sh --tier 2 # Non-interactive: 1=Quick, 2=Standard, 3=Power
```

**Step 2: Sync setup-mcp.sh to template**

```bash
cp scripts/setup-mcp.sh template/scripts/setup-mcp.sh
```

**Step 3: Update create-dld index.js tier references**

In file `packages/create-dld/index.js`, replace:
```javascript
    console.log('\nTier 3 servers require API keys.');
    console.log('Run ./scripts/setup-mcp.sh --tier 3 to complete setup.');
```
With:
```javascript
    console.log('\nPower tier servers require API keys.');
    console.log('Run ./scripts/setup-mcp.sh --tier 3 to complete Power setup.');
```

**Verification:**
```bash
grep "Tier [0-9]:" scripts/setup-mcp.sh
# Expected: 0 results

grep "Tier [0-9]:" template/scripts/setup-mcp.sh
# Expected: 0 results

diff scripts/setup-mcp.sh template/scripts/setup-mcp.sh
# Expected: no output (identical)
```

**Acceptance Criteria:**
- [ ] setup-mcp.sh uses Quick/Standard/Power in display labels
- [ ] setup-mcp.sh menu uses Quick/Standard/Power names
- [ ] create-dld index.js no longer says "Tier 3 servers"
- [ ] Root and template setup-mcp.sh are identical

---

### Task 6: Fix docs/00-bootstrap.md and docs/20-mcp-setup.md [BUG-083-05]

**Files:**
- Modify: `docs/00-bootstrap.md`
- Modify: `docs/20-mcp-setup.md`

**Context:**
Bootstrap doc references non-existent `ai/principles/` directory. MCP setup doc uses "Zero/Recommended" instead of Quick/Standard tier names. These are straightforward text fixes.

**Step 1: Remove ai/principles reference from bootstrap doc**

In file `docs/00-bootstrap.md`, replace:
```markdown
- [ ] Copied `ai/principles/` to the project
```
With:
```markdown
- [ ] Ran `npx create-dld` or installed DLD manually
```

**Step 2: Update minimal set section to remove principles reference**

In file `docs/00-bootstrap.md`, replace:
```
ai/
├── principles/          # Copy entirely
└── idea/               # Created by /bootstrap
```
With:
```
ai/
├── idea/               # Created by /bootstrap
│   ├── vision.md
│   ├── domain-context.md
│   ├── product-brief.md
│   └── architecture.md
└── backlog.md          # Task queue
```

**Step 3: Update "What to read" section**

In file `docs/00-bootstrap.md`, replace:
```
**Before /bootstrap (5 min):**
- This file

**After /bootstrap, before Day 1 (15 min):**
- `01-principles.md` — understand the philosophy
- `03-project-structure.md` — understand the structure
- `09-onboarding.md` — Day 1-4 checklist

**As needed:**
- Other documents — reference material
```
With:
```
**Before /bootstrap (5 min):**
- This file

**After /bootstrap, before Day 1 (15 min):**
- `docs/01-principles.md` -- understand the philosophy
- `docs/03-project-structure.md` -- understand the structure

**As needed:**
- Other documents in `docs/` -- reference material
```

**Step 4: Update MCP setup doc tier labels**

In file `docs/20-mcp-setup.md`, replace the tier table:
```markdown
| Tier | Servers | Setup Time | API Keys | Best For |
|------|---------|------------|----------|----------|
| **Zero** | None | 0 min | None | Quick evaluation |
| **Recommended** | Context7 + Exa | 2 min | None | Active development |
| **Power** | + Memory + Sequential | 5 min | Memory key | Teams, complex projects |
```
With:
```markdown
| Tier | Servers | Setup Time | API Keys | Best For |
|------|---------|------------|----------|----------|
| **Quick** | None | 0 min | None | Quick evaluation |
| **Standard** | Context7 + Exa | 2 min | None | Active development |
| **Power** | + Memory + Sequential | 5 min | Memory key | Teams, complex projects |
```

And replace:
```markdown
**Recommendation:** Start with **Recommended** tier. All features work, no API keys needed.
```
With:
```markdown
**Recommendation:** Start with **Standard** tier. All features work, no API keys needed.
```

And replace:
```markdown
## Tier 1: Zero (No MCP)
```
With:
```markdown
## Quick Tier (No MCP)
```

And replace:
```markdown
## Tier 2: Recommended (Context7 + Exa)
```
With:
```markdown
## Standard Tier (Context7 + Exa)
```

And replace:
```markdown
## Tier 3: Power (Teams & Complex Projects)
```
With:
```markdown
## Power Tier (Teams & Complex Projects)
```

And replace:
```markdown
Start with Tier 2, then add:
```
With:
```markdown
Start with Standard tier, then add:
```

And replace:
```markdown
**What you get (in addition to Tier 2):**
```
With:
```markdown
**What you get (in addition to Standard):**
```

**Verification:**
```bash
grep "ai/principles" docs/00-bootstrap.md
# Expected: 0 results

grep "Tier [0-9]:" docs/20-mcp-setup.md
# Expected: 0 results
```

**Acceptance Criteria:**
- [ ] `grep "ai/principles" docs/00-bootstrap.md` returns 0 results
- [ ] docs/20-mcp-setup.md uses Quick/Standard/Power labels
- [ ] Bootstrap minimal set section no longer references principles/

---

### Task 7: Create template/ai/ARCHITECTURE.md and fix README.md [BUG-083-05]

**Files:**
- Create: `template/ai/ARCHITECTURE.md`
- Modify: `README.md`

**Context:**
template/CLAUDE.md references `ai/ARCHITECTURE.md` (line 55: "See ai/ARCHITECTURE.md after bootstrap") but the file doesn't exist in the template. It should be a stub that gets populated by /bootstrap. README.md needs /bootstrap positioned more prominently as Day 0.

**Step 1: Create template/ai/ARCHITECTURE.md**

Create file `template/ai/ARCHITECTURE.md` with content:

```markdown
# Architecture

Generated after `/bootstrap`. Contains project-specific architecture decisions.

See `.claude/rules/architecture.md` for framework-level patterns.

---

## Domains

*Populated by /bootstrap*

## Dependencies

*Populated by /bootstrap*

## Entry Points

*Populated by /bootstrap*
```

**Step 2: Emphasize /bootstrap in README.md**

In file `README.md`, replace the section:
```markdown
### Option A: Ask Claude (Recommended)

Already have a project? Just say to Claude:

```
Install DLD from github.com/Ellevated/dld
```

Claude will scan your project, show what will change, and ask for confirmation before installing.

### Option B: CLI
```
With:
```markdown
### Option A: Ask Claude (Recommended)

Already have a project? Just say to Claude:

```
Install DLD from github.com/Ellevated/dld
```

Claude will scan your project, show what will change, and ask for confirmation before installing.

### Option B: CLI (New Project)
```

Then replace:
```markdown
That's it. Bootstrap will guide you through extracting your idea into structured specs.
```
With:
```markdown
**Day 0:** Run `/bootstrap` in Claude -- it will guide you through extracting your idea into structured specs. This is the most important step.
```

**Acceptance Criteria:**
- [ ] `template/ai/ARCHITECTURE.md` exists with stub content
- [ ] README.md has `/bootstrap` explicitly called out as Day 0

---

### Task 8: Fix smoke-test.sh, check-sync.sh, and sync scripts to template [BUG-083-04, 06]

**Files:**
- Modify: `scripts/smoke-test.sh`
- Modify: `scripts/check-sync.sh`
- Create: `template/scripts/smoke-test.sh` (copy from root after fixes)
- Create: `template/scripts/check-sync.sh` (copy from root after fixes)
- Create: `template/scripts/install-hooks.sh` (copy from root)

**Context:**
smoke-test.sh uses `node` without checking it exists and lists only 6 of 8 required hooks. check-sync.sh searches for `.py` files but hooks are now `.mjs`. Three scripts exist in root but not in template. This task fixes root scripts then copies them.

**Step 1: Add Node.js check to smoke-test.sh**

In file `scripts/smoke-test.sh`, after line 64 (`log_info "Starting smoke test for DLD template structure"`), but before `log_info "Template: $TEMPLATE_DIR"`, add:

Actually, insert the Node.js check right after the `log_info "Template: $TEMPLATE_DIR"` line (line 64), before `# Create temp directory`:

Replace:
```bash
log_info "Starting smoke test for DLD template structure"
log_info "Template: $TEMPLATE_DIR"

# Create temp directory
```
With:
```bash
log_info "Starting smoke test for DLD template structure"
log_info "Template: $TEMPLATE_DIR"

# Check Node.js availability (required for JSON validation)
if ! command -v node >/dev/null 2>&1; then
    log_fail "Node.js not found. Install Node.js 18+ to run smoke tests."
fi

# Create temp directory
```

**Step 2: Update REQUIRED_HOOKS list**

In file `scripts/smoke-test.sh`, replace:
```bash
REQUIRED_HOOKS=(
    "pre-bash.mjs"
    "pre-edit.mjs"
    "post-edit.mjs"
    "prompt-guard.mjs"
    "utils.mjs"
    "run-hook.mjs"
)
```
With:
```bash
REQUIRED_HOOKS=(
    "pre-bash.mjs"
    "pre-edit.mjs"
    "post-edit.mjs"
    "prompt-guard.mjs"
    "session-end.mjs"
    "validate-spec-complete.mjs"
    "utils.mjs"
    "run-hook.mjs"
)
```

**Step 3: Add .gitignore and README.md checks**

In file `scripts/smoke-test.sh`, after the root files check (after `log_pass "Root files present and non-empty"`), add a new test:

Replace:
```bash
log_pass "Root files present and non-empty"

# Test 2: .claude/settings.json
```
With:
```bash
log_pass "Root files present and non-empty"

# Test 1b: README.md and .gitignore
log_test "Checking README.md and .gitignore"
if [ ! -f "$TEST_PROJECT/README.md" ]; then
    log_fail "Missing README.md"
fi
if [ ! -f "$TEST_PROJECT/.gitignore" ]; then
    log_fail "Missing .gitignore"
fi
log_pass "README.md and .gitignore present"

# Test 2: .claude/settings.json
```

**Step 4: Fix check-sync.sh to include .mjs files**

In file `scripts/check-sync.sh`, replace all three occurrences of the find pattern.

Replace:
```bash
  <(find .claude -type f \( -name "*.md" -o -name "*.py" -o -name "*.json" \) 2>/dev/null | sed 's|^.claude/||' | sort) \
  <(find template/.claude -type f \( -name "*.md" -o -name "*.py" -o -name "*.json" \) 2>/dev/null | sed 's|^template/.claude/||' | sort)
```
(first occurrence) With:
```bash
  <(find .claude -type f \( -name "*.md" -o -name "*.mjs" -o -name "*.json" \) 2>/dev/null | sed 's|^.claude/||' | sort) \
  <(find template/.claude -type f \( -name "*.md" -o -name "*.mjs" -o -name "*.json" \) 2>/dev/null | sed 's|^template/.claude/||' | sort)
```

Replace second occurrence (under "Files only in template"):
```bash
  <(find .claude -type f \( -name "*.md" -o -name "*.py" -o -name "*.json" \) 2>/dev/null | sed 's|^.claude/||' | sort) \
  <(find template/.claude -type f \( -name "*.md" -o -name "*.py" -o -name "*.json" \) 2>/dev/null | sed 's|^template/.claude/||' | sort)
```
With:
```bash
  <(find .claude -type f \( -name "*.md" -o -name "*.mjs" -o -name "*.json" \) 2>/dev/null | sed 's|^.claude/||' | sort) \
  <(find template/.claude -type f \( -name "*.md" -o -name "*.mjs" -o -name "*.json" \) 2>/dev/null | sed 's|^template/.claude/||' | sort)
```

Replace the line count diff pattern:
```bash
for f in $(find template/.claude -type f \( -name "*.md" -o -name "*.py" \) 2>/dev/null); do
```
With:
```bash
for f in $(find template/.claude -type f \( -name "*.md" -o -name "*.mjs" -o -name "*.json" \) 2>/dev/null); do
```

**Step 5: Copy fixed scripts to template**

```bash
cp scripts/smoke-test.sh template/scripts/smoke-test.sh
cp scripts/check-sync.sh template/scripts/check-sync.sh
cp scripts/install-hooks.sh template/scripts/install-hooks.sh
chmod +x template/scripts/smoke-test.sh
chmod +x template/scripts/check-sync.sh
chmod +x template/scripts/install-hooks.sh
```

**Verification:**
```bash
# Verify Node.js check in smoke-test
grep "command -v node" scripts/smoke-test.sh
# Expected: match found

# Verify 8 hooks listed
grep -c ".mjs" scripts/smoke-test.sh
# Expected: 8 (one per hook in REQUIRED_HOOKS)

# Verify .mjs in check-sync
grep "\.mjs" scripts/check-sync.sh
# Expected: matches found

# Verify no .py in check-sync find commands
grep '\.py' scripts/check-sync.sh
# Expected: 0 results

# Verify template scripts exist
ls -la template/scripts/
# Expected: smoke-test.sh, check-sync.sh, install-hooks.sh, ci-status.sh, setup-mcp.sh, autopilot-loop.sh
```

**Acceptance Criteria:**
- [ ] smoke-test.sh checks for Node.js before using it
- [ ] REQUIRED_HOOKS lists all 8 hooks (including session-end.mjs and validate-spec-complete.mjs)
- [ ] smoke-test.sh validates README.md and .gitignore existence
- [ ] check-sync.sh searches for .mjs files (not .py)
- [ ] `template/scripts/smoke-test.sh` exists (identical to root)
- [ ] `template/scripts/check-sync.sh` exists (identical to root)
- [ ] `template/scripts/install-hooks.sh` exists (identical to root)
- [ ] All template scripts are executable

---

### Execution Order

```
Task 1 (create template files)
  ↓
Task 2 (fix template/CLAUDE.md)
  ↓
Task 3 (security: settings.json + hook utils + run-hook)
  ↓
Task 4 (hook logic: minimatch + inferSpec + extractAllowed + pre-bash + sync to root)
  ↓
Task 5 (tier labels: setup-mcp.sh + create-dld)
  ↓
Task 6 (docs: bootstrap + mcp-setup)
  ↓
Task 7 (create ARCHITECTURE.md + README.md)
  ↓
Task 8 (smoke-test + check-sync + template scripts sync)
```

All tasks are SEQUENTIAL. Task 3 and 4 both modify `template/.claude/hooks/utils.mjs` and `template/.claude/hooks/run-hook.mjs` -- Task 3 adds security functions, Task 4 adds logic fixes and then syncs to root. Task 8 copies scripts to template, so Tasks 1 and 5 must complete first (they create/modify scripts).

### Dependencies

- Task 2 depends on nothing (standalone file edit)
- Task 3 depends on nothing (standalone edits to hooks)
- Task 4 depends on Task 3 (modifies same files, adds more changes, does final sync)
- Task 5 depends on nothing (different files)
- Task 6 depends on nothing (different files)
- Task 7 depends on nothing (different files)
- Task 8 depends on Tasks 1, 4, and 5 (copies scripts to template after all fixes)

### Sub-Spec Coverage

| Sub-Spec | Tasks |
|----------|-------|
| BUG-083-01 (Template Missing Files) | Task 1, Task 2 |
| BUG-083-02 (Security Defaults) | Task 3 |
| BUG-083-03 (Tier Numbering) | Task 2, Task 5, Task 6 |
| BUG-083-04 (Hook Logic) | Task 4, Task 8 |
| BUG-083-05 (Documentation) | Task 2, Task 6, Task 7 |
| BUG-083-06 (Smoke Test) | Task 8 |

### Allowed Files (Complete List)

```
template/README.md                          (create - Task 1)
template/.gitignore                         (create - Task 1)
template/scripts/ci-status.sh              (create - Task 1)
template/CLAUDE.md                          (modify - Task 2)
template/.claude/settings.json             (modify - Task 3)
template/.claude/hooks/utils.mjs           (modify - Tasks 3, 4)
template/.claude/hooks/run-hook.mjs        (modify - Tasks 3, 4)
template/.claude/hooks/pre-bash.mjs        (modify - Task 4)
.claude/hooks/utils.mjs                    (sync from template - Task 4)
.claude/hooks/run-hook.mjs                 (sync from template - Task 4)
.claude/hooks/pre-bash.mjs                 (sync from template - Task 4)
scripts/setup-mcp.sh                       (modify - Task 5)
template/scripts/setup-mcp.sh             (sync from root - Task 5)
packages/create-dld/index.js              (modify - Task 5)
docs/00-bootstrap.md                       (modify - Task 6)
docs/20-mcp-setup.md                       (modify - Task 6)
template/ai/ARCHITECTURE.md               (create - Task 7)
README.md                                  (modify - Task 7)
scripts/smoke-test.sh                      (modify - Task 8)
scripts/check-sync.sh                      (modify - Task 8)
template/scripts/smoke-test.sh            (sync from root - Task 8)
template/scripts/check-sync.sh            (sync from root - Task 8)
template/scripts/install-hooks.sh         (sync from root - Task 8)
```
