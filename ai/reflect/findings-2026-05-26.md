# Reflect Findings — 2026-05-26

Session covers upstream signals 2026-05-23..2026-05-25 (4 signals) +
template-sync correction (2026-02-16). Diary entries already processed in
previous reflect session (2026-03-18); only signals are pending.

No frequency≥3 patterns in raw data, but signals arrive pre-synthesized
with concrete suggested actions — surfaced for Hermes triage.

---

## F1: Spec Allowed Files миссят `template/.claude/` зеркала

**Frequency:** 2 occurrences (different angles, same root: template-sync awareness).
**Evidence:**
- SIGNAL-2026-05-25-1340 — ARCH-193 spec включил `.claude/hooks/pre-commit-lifecycle-guard.mjs` в Allowed Files но пропустил `template/.claude/hooks/pre-commit-lifecycle-guard.mjs`. Три других template/.claude/* зеркала БЫЛИ перечислены — омисcия, не intent. Coder синхронизировал оба per template-sync.md, форсировав middle-flow правку Allowed Files оператором.
- 2026-02-16 correction — я редактировал `template/.claude/`, а тестировал из `.claude/`. Обратная асимметрия того же gap.

**Type:** failure_pattern / process gap

**Proposed action:**
- Phase 5.5 SSOT lint (или Spark facilitator checklist): когда `.claude/<path>` добавляется в Allowed Files И существует matching `template/.claude/<path>` — auto-include зеркало (или fail-loud с подсказкой).
- Implementation hint: `grep -F "$path" .claude/rules/template-sync.md` + `test -f template/$path` в Spark Phase 5.5.

**Risk if ignored:** Coder либо синкает молча (хорошо но скрытно), либо не синкает (template drifts), либо blocks autopilot на правку Allowed Files. Все три исхода — friction.

---

## F2: Devil scout returns text вместо Write tool

**Frequency:** 2 из последних 3 spark-сессий (SIGNAL-2026-05-23-2300-process).
**Evidence:**
- SIGNAL-2026-05-23-2300-process — Devil scout вернул анализ как `result` (текст), остальные 3 scouts записали файлы корректно. ADR-007 fallback сработал — facilitator записал `research-devil.md` из response text — но это специфический drift Devil.

**Type:** prompt_gap / agent drift

**Proposed action:**
- Обновить `.claude/agents/spark/devil.md` (и `template/.claude/agents/spark/devil.md`) добавив explicit рекомендацию по образцу `spark/completion.md:298`: "MUST use Write tool — текстовый ответ без файла = DATA LOSS". Текущий промт, видимо, недостаточно жёсткий именно для Devil.

**Risk if ignored:** Low — ADR-007 fallback покрывает. Но при дальнейшем drift Devil meta-cost compound: facilitator пишет за scout = дополнительный round-trip context.

---

## F3: `ai/architect/migration-path.md` MP-001 рисует gate как "single --grep rule" (TECH-177 regression risk)

**Frequency:** 1 (но severity warning — может re-introduce целый класс инцидентов).
**Evidence:**
- SIGNAL-2026-05-23-2300 — MP-001 описание lines 39-59 говорит: "Single rule gate: `git log origin/develop --grep \"SPEC-ID\"` (touching allowed_files)". Bare `--grep` матчит body/trailer (Devil scout Attack 2 + Attack 10) = pre-TECH-177 поведение.
- Callback сегодня делает two-step: path-filter via `git log -- <paths>`, потом Python subject-only `_subject_implements` (callback.py:673-711, 734-775).
- ARCH-190 spec explicitly REJECTS bare --grep (Task 1 acceptance criterion + EC-1/EC-3/EC-9 тесты).
- Также: `_spec_has_merged_implementation` переименован в `_is_done_on_develop` (commit cefaa55, 2026-05-21) — docs ссылаются на устаревшее имя.

**Type:** doc_drift / regression_risk

**Proposed action:**
- Architect обновляет `ai/architect/migration-path.md` MP-001 описание — заменить "single grep rule" формулировку на two-step (path-filter → subject-only match). Точный текст замены в SIGNAL-2026-05-23-2300 "Suggested Action".
- Бонус: глобальный grep `_spec_has_merged_implementation` → заменить на `_is_done_on_develop` в migration docs.

**Risk if ignored:** Future operator/agent читает только architectural summary, реализует "single --grep" → TECH-177 incident class re-emerges (false-done на body/trailer matches).

---

## F4: Spec-reviewer пропускает inline scope relaxation

**Frequency:** 1 эпизод (SIGNAL-2026-05-25-1340-2), но categoral.
**Evidence:**
- Spec-reviewer agent отметил Test 8 (ARCH-193 Task 9) как `needs_implementation — event_writer.notify not verified`, но спека explicitly разрешает structural-only fallback в строке 577: "Practical approach: directly call lifecycle.write_lifecycle(done→blocked) ... full integration is overkill".
- Reviewer матчанул test name + первое предложение, пропустив inline scope relaxation внутри test description.

**Type:** prompt_gap / reviewer over-strictness

**Proposed action:**
- Обновить `.claude/agents/spec-reviewer.md` (+ template зеркало) — добавить explicit инструкцию: "Read the FULL test description in the spec — особенно watch for 'Practical approach' / 'overkill' / 'simplification' / 'structural only' phrases that scope down the requirement. Не флагать deviation если спека сама authorized это."

**Risk if ignored:** Reviewer ложно требует "implementation" того что спека ослабила → autopilot debate loop → blocked task где работа уже корректна.

---

## Notes (not findings)

- **Не findings**, но фиксирую: corrections.md 2026-05-20 ("убери ACTION REQUIRED метку") не имеет соответствующего `feedback_*.md` файла в `~/.claude/projects/-home-dld-projects-dld/memory/`. Возможно стоит добавить — но это не reflect-задача, а memory hygiene.
- **Stale signals:** SIGNAL-001..010 (2026-02-28, Architect Board) — Briefcase/OAuth контекст, вероятно от другого этапа DLD-как-продукта; >3 мес давности. Не включены — Hermes решает архивировать ли upstream-signals.md head.
