# Reflect Findings — 2026-07-28

Session covers 2026-07-12..2026-07-28 (16 days since last reflect).

**Sources analyzed:**
- 4 pending diary entries (TECH-211, 2026-07-27: 3 success + 1 advisory)
- 2 new upstream signals (`SIGNAL-20260727-spark-ARCH-209`, `SIGNAL-20260727-spark-lessons-bank`)
- Carry-forward verification of findings-2026-07-12 (F1..F4)
- No new memory files since 2026-07-02

---

## F1: Инварианты держатся комментарием, а не тестом

**Frequency:** 4 occurrences, 3 независимых источника. **Evidence:** TECH-211, ARCH-209, F4@2026-07-12.
**Type:** failure_pattern / design_decision

- `lifecycle_audit.py:61-65` — алиасы `CATEGORIES`/`_parse_backlog_columns` живут только
  за счёт комментария; ничто не проверяет, что их не удалят. Поломка всплывёт в
  out-of-scope `test_orchestrator_bootstrap.py` (diary TECH-211 advisory).
- `SIGNAL-20260727-spark-ARCH-209` — `AFTER <ID>`-маркеры в `ai/backlog.md` переживают
  запись статуса только потому, что `sync_status` бережёт лишние байты. Ни одного теста.
  Один запуск `render_backlog.render_backlog()` молча стирает граф зависимостей проекта,
  и `_backlog_deps` на пустом множестве отпустит спеку в диспатч раньше срока.
- `.claude/rules/dependencies.md:641` — карта зависимостей описывает `lifecycle_audit.py`
  как единый модуль; `audit_probe`/`audit_categories`/`reaper_pueue`/`reaper_liveness`
  в ней отсутствуют. Ничто не сверяет карту с деревом модулей → Impact Tree по
  `LIFECYCLE_DIR` укажет на неверный файл.
- Reinforces F4@2026-07-12: дублированная subject-matching логика в `callback.py` и
  `gate_logic.py` синхронизируется соглашением. TECH-210 (dedup) заведена, но
  **blocked** (`autopilot_signaled_blocked`, 2026-07-27) — то есть корень до сих пор жив.

**Proposed action:** ввести класс регрессионных тестов «инвариант, а не поведение»:
(1) `AFTER`-маркер переживает `write_lifecycle`; (2) публичные алиасы модуля присутствуют;
(3) fitness-тест «каждый модуль в `scripts/vps/` упомянут в `dependencies.md`».
ARCH-209 — естественный владелец, поскольку раскол модулей продолжается.

---

## F2: Allowed Files порождает бесхозный долг — advisory некуда маршрутизировать

**Frequency:** 5 из 5 строк advisory-отчёта TECH-211. **Evidence:** `ai/diary/2026-07-27-TECH-211-advisory.md`.
**Type:** tool_workflow

Каждая строка advisory заканчивается формулой «файл вне Allowed Files → отдать в ARCH-209
или отдельной спекой». Ни одна не превратилась в задачу: `dependencies.md` осталась
устаревшей, 12 дублирующихся тестов в `test_orchestrator_bootstrap.py` остались, стухший
docstring `heartbeat_reaper.py:5,24` остался. Advisory-файл — терминал, а не очередь:
у него нет ни ID, ни статуса, ни читателя, кроме reflect'а раз в две недели.

Scope-дисциплина здесь работает правильно (автопилот не полез за границы) — дыра в том,
что у корректно отклонённой правки нет дешёвого пути стать задачей.

**Proposed action:** автопилот при записи advisory должен создавать либо одну follow-up
запись в backlog со ссылкой на advisory-файл, либо `after: <текущая спека>` в уже
существующей зонтичной спеке (ARCH-209). Альтернатива дешевле: reflect обязан
конвертировать advisory-строки в inbox-кандидаты — но тогда задержка до 2 недель.

---

## F3: `orchestrator.git_pull` до сих пор без rebase-recovery (carry-forward, 3-я сессия)

**Frequency:** 5+ инцидентов (2026-06-21/22), не исправлено, спеки в backlog нет.
**Evidence:** `orchestrator.py:298` — `git merge --ff-only origin/develop`, при неудаче
`"... failed for %s: %s — skip cycle"` (`:305`). Симметричный фикс в `_push_best_effort`
(de4f434, 2026-06-21) не портирован. Подтверждено grep'ом 2026-07-28.
**Type:** failure_pattern

F3@2026-07-12 назвала это «Defect C (UNFIXED)». За 16 дней задача не заведена — в
`ai/backlog.md` нет ни одного упоминания `ff-only`/`git_pull`/`rebase`/divergence.
Пока проект не расходится, дефект невидим; когда разойдётся — оркестратор навсегда
уходит в «skip cycle» по этому проекту до ручного `reset --hard`.

**Proposed action:** завести спеку (TECH). Порт логики de4f434 в `git_pull` с теми же
guardrails: при провале ff-only проверить, что локальное опережение — только lifecycle,
затем rebase + retry. Наблюдаемость: логировать divergence как WARNING, не как рутинный
«skip cycle», иначе следующий случай снова утонет в шуме.

---

## F4: Общее правило про необратимые действия так и не попало в CLAUDE.md (carry-forward)

**Frequency:** 3 инцидента (tmux ×1, rebase/ugc.db ×1, ACTION REQUIRED ×1), новых нет.
**Evidence:** `grep -in "destructive|irreversible|необратим"` по `CLAUDE.md` и
`~/.claude/CLAUDE.md` → единственное попадание — определение R0 в таблице рисков, то есть
классификатор для Spark Phase 4, а не правило поведения агента.
**Type:** user_preference (STRONG)

Три частных memory-файла (`feedback_never-kill-tmux-windows`,
`feedback_no-rebase-with-divergent-tracked-files`, corrections 2026-05-20) существуют,
общего правила нет. Memory-файлы грузятся не в каждую сессию и покрывают ровно тот
инструмент, на котором обожглись, — четвёртый способ что-то снести они не ловят.

**Proposed action (без изменений с 2026-07-12):** в `## Key Rules` добавить принцип
«при неоднозначном запросе выбирать неразрушающее прочтение; необратимое — только после
явного да». Частные memory остаются примерами.

---

## F5: Gate 7 (Historical Risks) — обязательная секция, которая никогда не срабатывает

**Frequency:** 9 спек за один день (2026-07-27), гейт авто-прошёл во всех.
**Evidence:** `SIGNAL-20260727-spark-lessons-bank`; `ls -la ai/lessons/` → только
`.gitkeep`, 0 байт, 2026-05-10 (проверено 2026-07-28, за 79 дней не изменилось).
**Type:** tool_workflow

`feature-mode.md` Gate 7 авто-пропускает, «если `ai/lessons/` не существует». Директория
существует и пуста — формально это даже не описанный в гейте случай. Секция
`## Historical Risks` присутствует в каждой спеке и в каждой пуста. Гейт, который не
срабатывал ни разу, неотличим от отсутствующего, и приучает пропускать секцию глазами.
При этом дефектный след по `scripts/vps/` богат (git-история, docstring'и) — 2026-07-27
его собирали вручную по каждой спеке.

**Proposed action:** одно из двух, не оба. (а) запустить `/seed-lessons` (скилл и триггер
уже существуют) и засеять банк по архиву BUG-спек; (б) снять секцию из шаблона спеки.
Развилка — за Architect/founder'ом.

---

## Process notes (not findings)

- **Exa кредиты исчерпаны второй сессией подряд** (402 на обоих запросах, 2026-07-28).
  Внешняя валидация findings не выполнялась с 2026-06-21. Требуется top-up на
  dashboard.exa.ai либо явное решение, что reflect работает без research-шага.
- **TECH-210 blocked** с 2026-07-27 (`autopilot_signaled_blocked`) — это фикс корня
  F4@2026-07-12. Нужен разбор причины блокировки, иначе самая патчимая поверхность
  оркестратора остаётся дублированной.
- **Advisory-файлы не индексируются** в `ai/diary/index.md` как отдельный тип — та же
  болезнь, что у `corrections.md` до прошлой сессии. См. F2.
- **Устаревшая голова `upstream-signals.md`:** SIGNAL-001..010 (2026-02-28) — из другой
  фазы продукта, 5 месяцев. По-прежнему не заархивированы (третье упоминание).

---
---

# Session 2 — 2026-07-28 (после TECH-212)

Вторая reflect-сессия за день: TECH-212 завершилась уже после записи findings выше.

**Sources analyzed:**
- 5 записей дневника TECH-212 (3 success + 2 advisory)
- 6 новых upstream-сигналов `SIGNAL-2026-07-28-0130..0135`
- Перепроверка F1..F5 из сессии 1

---

## G1: Design-секция спеки — неисполненный код, который ловит только планировщик

**Frequency:** 4 из 6 сигналов TECH-212 (0130, 0131, 0132, 0133). **Type:** failure_pattern

Четыре независимых дефекта в одной спеке, все — в разделах, которые исполнитель обязан
читать буквально:

1. **Неработающий код в Design.** `TECH-212:187-193` предлагает
   `db_decisions.record_decision(get_db(), ...)`. `get_db` — `@contextmanager`
   (`scripts/vps/db.py:52`), то есть в лист ушёл бы `_GeneratorContextManager` вместо
   `Connection`, и **ни одна запись никогда бы не закоммитилась**. Тихая потеря данных,
   которую не поймал бы ни один существующий тест.
2. **Потерянный контракт транзакции.** `immediate=True` (BEGIN IMMEDIATE) — часть
   контракта `clear_decisions` / `save_finding` / `update_finding_status`
   (`git show HEAD~4:scripts/vps/db.py:142,163,343,374`). Спека о нём не упоминает;
   дословный перенос дал бы race-condition. В итоговом коде флаг выжил только потому,
   что планировщик ввёл `_delegate(fn, immediate=...)` (`db.py:333,352,358,360`).
3. **Eval Criteria противоречат Design.** EC-7 требует `grep 'f"SELECT'` → 0 попаданий,
   а план «дословного переноса» тащил `f"SELECT ... IN ({placeholders})"` из
   `db.py:527`. Спека, выполненная буквально, проваливала собственную приёмку.
4. **LOC-бюджет оценён на глаз и ошибся в ~5 раз.** «Делегат стоит одну строку»
   (`TECH-212:163`) — 12 рукописных `def`-делегатов дали бы 60–72 строки и вывели
   `db.py` на 395–407 при лимите 400. Спасла `_delegate`-фабрика; итог 373.

Общий корень: **Spark пишет код в Design и критерии в Eval, не исполняя ни то, ни другое,
и не сверяя их между собой.** Impact Tree смотрит на имена и импорты — не на семантику
транзакций и не на тип возвращаемого значения. Единственный барьер — то, что планировщик
перепроверил вместо копирования. Барьер не гарантирован: агент, который «доверяет спеке»,
здесь молча ломает БД.

**Proposed action:** для спек с кодом в Design — либо (а) фаза самопроверки в Spark:
прогнать grep'ы собственных Eval Criteria по текущему коду и сверить сигнатуры вызываемых
функций (`@contextmanager`, обязательные kwargs); либо (б) явная маркировка сниппетов как
`# ILLUSTRATIVE — verify before copying`, чтобы исполнитель не имел права копировать
дословно. Владелец — Spark (`feature-mode.md`).

---

## G2: CI-джоб `python-lint` красный на develop, и это никого не будит

**Frequency:** 3-й рецидив темы CI-parity (memory `autopilot-ci-parity-gap`: TECH-206
2026-06-21, миграционный concurrency-cancel 2026-06-28, теперь линтер). **Type:** failure_pattern

Проверено 2026-07-28 на develop:

| Команда | Результат |
|---|---|
| `ruff check .` | **Found 13 errors** (12× F401 unused-import, 1× F541) |
| `ruff format --check .` | **17 files would be reformatted** (72 ok) |
| `.github/workflows/ci.yml` job `python-lint` | выполняет **обе** команды (`:55`, `:56`) |
| `command -v ruff` | **NOT on PATH** — только `~/.local/bin/ruff`, вне venv проекта |
| `ruff` в `.claude/agents/tester.md` / `skills/autopilot/*` | **0 упоминаний** |

Сигнал 0135 ошибся в детали (утверждал, что джоб делает «ровно `ruff format --check .`»)
— на деле обе команды, и **обе красные**. Значит `python-lint` падает на каждом push в
develop уже некоторое время, и ни один агент этого не сообщает: локальный гейт линтер не
включает вообще, а `ruff` не на PATH, так что агент не проверит формат, даже если захочет.
13 ошибок — `[*] 13 fixable with --fix`, то есть долг чисто механический и копится только
потому, что никто не смотрит.

Отличие от TECH-206: тот закрывал parity по **тестам**. Линтер остался снаружи, и дыра
воспроизвела ровно тот же класс отказа — «зелёный локально, красный в CI, никто не видит».

**Proposed action:** (1) разовый `ruff check --fix . && ruff format .` (механика, ~0 риска);
(2) `ruff` в зависимости проекта/venv, чтобы был на PATH у агентов;
(3) расширить локальный гейт до полного набора CI-команд, а не подмножества — иначе
следующий добавленный в CI шаг снова окажется невидимым.

---

## G3: Exa исчерпан — research-стек всех агентов деградирует молча

**Frequency:** 3 подряд reflect-сессии с 402 (2026-07-12, 2026-07-28 s1, 2026-07-28 s2)
+ независимое попадание из planner TECH-212 (`SIGNAL-2026-07-28-0134`). **Type:** failure_pattern

Повышаю из «process note» в finding: за 16 дней подтвердилось, что это не разовый сбой
reflect'а, а системная деградация. `web_search_exa` → HTTP 402 на всех вызовах. Затронуты
`planner`, `scout`, `spark-research`, `bughunt-solution-architect`, `board-*`,
`architect-*`, `council-*` — то есть весь research-слой DLD.

Отказ **тихий**: агент получает ошибку, не падает, продолжает работать «по памяти» и
выдаёт результат обычного вида. Отличить сессию с research от сессии без него по выходу
невозможно. `scripts/check-research-stack.py` существует ровно для этого, но его ничто не
вызывает периодически — CLAUDE.md предлагает запускать вручную «когда качество кажется
плохим», а качество без внешних источников кажется нормальным.

**Proposed action:** развилка за founder'ом: (а) top-up на dashboard.exa.ai; (б) явное
решение, что DLD работает без Exa, и тогда убрать её из tool-списков агентов, чтобы не
тратить ходы на заведомо падающие вызовы. Независимо от выбора — `check-research-stack.py`
должен вызываться автоматически (ночным ревьюером или health-циклом оркестратора) и
шуметь при деградации, а не ждать, пока кто-то заподозрит.

---

## Carry-forward: что изменилось с сессии 1 (тот же день)

- **F1 / F2 частично закрыты по TECH-212.** `.claude/rules/dependencies.md:82-84,104-105,753`
  теперь описывает `db_decisions` / `db_findings` / `db_cli` с сохранением `immediate=True`
  в комментарии. **Но** модули TECH-211 (`audit_probe`, `audit_categories`, `reaper_pueue`,
  `reaper_liveness`) в карте по-прежнему отсутствуют. Advisory TECH-212 предупреждала о том
  же — и на этот раз кто-то обновил карту вручную. Итог: маршрут advisory→задача так и не
  существует, дыра просто закрыта разовым усилием. F2 остаётся в силе.
- **F1 (fitness-тест «каждый модуль упомянут в dependencies.md») подтверждён практикой:**
  ровно этот тест поймал бы пробел TECH-211.
- **TECH-210 разблокирована** — в `ai/backlog.md:23` статус `queued` (в сессии 1 было
  `blocked`/`autopilot_signaled_blocked`). Дедуп gate-логики снова в очереди.
- **F5 без изменений:** `ai/lessons/` — только `.gitkeep`, 0 байт, 2026-05-10.

## Ниже порога (frequency < 3, зафиксировано, в findings не выносится)

- **Мёртвый код перенесён как есть:** `get_finding_by_id` / `get_all_findings` — ноль
  потребителей, переехали в `db_findings.py` вместе с делегатами в `db.py:361-362`.
  Раскол по LOC консервирует мёртвый код вместо его удаления. Второе наблюдение того же
  рода (первое — дублирующиеся тесты в TECH-211); при третьем — в findings.
