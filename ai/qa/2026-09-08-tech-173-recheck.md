# QA Report: TECH-173 — Orchestrator docs (re-check спустя 4 месяца)

**Date:** 2026-09-08
**Environment:** локальные доки DLD (docs/orchestrator/, ~/.claude/projects/-root/memory/, CLAUDE.md ×2, .claude/rules/architecture.md) + гейт scripts/vps/check-doc-references.sh
**Trigger:** `/qa TECH-173` — спека закрыта 2026-05-02, проверяем, что читатель сегодня получает ценность

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 8     | 5    | 3    | 0       |

Контент спеки доставлен, но **переехал** (2026-06-26, `docs/orchestrator/`), а половина указателей
на него не переехала. Читатель, входящий не через корневой CLAUDE.md, попадает в тупик.

## Failures

### F1: Lint-гейт EC-7 падает молча — exit 1 без причины
**Severity:** Major · **Reproducibility:** Always (до фикса)
**Expected:** `bash scripts/vps/check-doc-references.sh` → либо «All checks passed», либо список MISSING.
**Actual:** печатал 2 строки OK и умирал с exit 1 без единого объяснения.
**Причина симптома:** `set -e` + `grep` без совпадений (TECH-токенов в файле-заглушке нет) → скрипт
обрывался на середине, проверки указателей вообще не выполнялись.
**User impact:** гейт, поставленный ловить дрейф доков, 4 месяца не мог назвать ни одной проблемы.
**Статус:** починен в этой сессии (см. Fixes Applied). После фикса гейт честно показывает F3.

### F2: template/CLAUDE.md вёл на снесённый файл и описывал снесённую модель
**Severity:** Major · **Reproducibility:** Always (до фикса)
**Expected:** блок «DLD Orchestrator Reference» в template == корневому (docs/orchestrator/).
**Actual:** template ссылался на `~/.claude/projects/-root/memory/dld-orchestrator.md` +
`orchestrator-runbook.md` (оба с 26.06 — MOVED-заглушки) и описывал критический путь как
«callback.py → verify_status_sync → plumbing commit» (модель ADR-018, снесена ARCH-186/ADR-023).
**User impact:** каждый проект, бутстрапнутый из template, получал ссылку в никуда + неверную
модель статусов. Классический template-drift: корень поправили 26.06, template — нет.
**Статус:** починен в этой сессии.

### F3: 9 живых указателей `dld-orchestrator.md§N` ведут в файл без секций
**Severity:** Minor · **Reproducibility:** Always
**Expected:** `См. dld-orchestrator.md§6` открывает раздел с ADR-индексом.
**Actual:** файл — 25-строчная MOVED-заглушка. Секций §5/§6/§8/§9 нет.
**Где:** `.claude/rules/architecture.md` — строки 77, 102-105, 107-109 (8 строк ADR-таблицы);
`docs/adr-archive.md:20`.
**Evidence:** после фикса F1 гейт печатает `MISSING pointer dld-orchestrator.md§6/§8/§9`, exit 1.
**User impact:** агент, читающий ADR-таблицу (а это always-loaded rule), идёт по ссылке и получает
заглушку. Таблица переадресации в заглушке спасает (§5–6 → status-model.md), но это лишний хоп.
**Не чиню:** 9 правок + сам гейт надо перенацелить на `docs/orchestrator/` — это спека, не light fix.

## Passed

| # | Сценарий | Итог |
|---|----------|------|
| 1 | EC-1: все 10 разделов доступны читателю | ✅ в перенесённом виде: README §1-§6, components.md, status-model.md, runbook.md, verification.md |
| 2 | EC-2: каждый компонент §4 имеет путь + описание | ✅ components.md: orchestrator.py, run-agent.sh, claude-runner.py, db.py+schema.sql, event_writer.py, gate-daemon.py + side monitors |
| 3 | EC-3: контракт callback и guard описаны актуально | ✅ status-model.md §7 + §1-§6 — уже под lifecycle-SoT (ADR-023), не под снесённой markdown-моделью |
| 4 | EC-4: ADR-индекс актуален | ✅ README §5 |
| 5 | EC-5: runbook покрывает 6 сценариев | ✅ покрыто 7 + pre-launch checklist + drift-toolkit |
| 6 | Корневой CLAUDE.md ведёт читателя туда, где доки лежат сейчас | ✅ строки 333-345 |
| 7 | EC-6 (субъективно): хватит ли новому человеку | ✅ да по содержанию — при условии, что он входит через корневой CLAUDE.md, а не через ADR-таблицу (F3) |

## Fixes Applied

| # | Что | Файл | Commit |
|---|-----|------|--------|
| 1 | `\|\| true` на трёх grep-присваиваниях — гейт перестал умирать молча | scripts/vps/check-doc-references.sh:36,49,67 | 59df0fe9 |
| 2 | Блок «DLD Orchestrator Reference» зеркалит корневой → docs/orchestrator/ | template/CLAUDE.md:370-381 | 59df0fe9 |

Фикс 2 — 11 строк, выше планки light-fix в 5 LOC. Сделан осознанно: чистый указатель в доке,
R2, иначе каждый новый проект из template продолжает получать мёртвую ссылку.

## Ещё замечено (не баги TECH-173)

- `check-doc-references.sh` **не подключён ни к одному CI-workflow** — вопреки задаче T7 «Запуск в CI».
  Гейт существует, но никогда не запускался автоматически. Это и есть причина, почему F1 прожил 4 месяца.
- В `ai/qa/` лежит **четыре** отчёта по TECH-173 от 2026-05-02 с одинаковым вердиктом PASS.
- `docs/orchestrator/callback-lifecycle-contour-to-be.md` (29 КБ, «to-be») не перечислен в индексе
  CLAUDE.md — читатель может принять его за описание текущего состояния.

## Вердикт по спеке

TECH-173 остаётся **done** — заявленная ценность (единый SoT по оркестратору) сегодня существует
и актуальна, просто по другому адресу. Демоут не требуется. Требуется добивка указателей (F3) и
подключение гейта в CI.
