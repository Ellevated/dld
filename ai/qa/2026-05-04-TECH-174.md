# QA Report: TECH-174 Manual Spec Verification Protocol

**Date:** 2026-05-04
**Environment:** local CLI (scripts/vps/, ~/.claude/projects/-root/memory/)
**Trigger:** /qa TECH-174
**Spec status at QA time:** `queued` (not yet implemented end-to-end)

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 7     | 5    | 2    | 0       |

CI/Deploy gate: N/A — operator-tooling spec, no deploy target.

## Failures

### F1: Protocol document missing (Task 1, EC-4)

**Severity:** Major
**Reproducibility:** Always
**Expected:** `~/.claude/projects/-root/memory/spec-verification-protocol.md` exists with `# Manual Spec Verification Protocol` heading, 7 `## Step N` sections, and ≥1 fenced bash block; `test_protocol_doc_has_seven_steps` passes.
**Actual:** File does not exist. Pytest skips the assertion test.
**Steps to reproduce:**
1. `ls ~/.claude/projects/-root/memory/spec-verification-protocol.md` → "No such file or directory"
2. `python3 -m pytest tests/integration/test_spec_verify.py::test_protocol_doc_has_seven_steps -v` → SKIPPED
**Evidence:** `ls` exit non-zero; pytest output `1 skipped`.
**User impact:** Operator has no checklist to follow when auditing a suspicious "done" spec — the whole point of TECH-174.
**Hint:** Task 1 of the implementation plan was not executed. Drift Log line 122 confirms the file was planned to be CREATEd.

### F2: `/qa` SKILL.md has no reference to protocol (Task 5)

**Severity:** Minor
**Reproducibility:** Always
**Expected:** `.claude/skills/qa/SKILL.md` contains references to `spec-verification-protocol.md`, `spec_verify.py`, and `spec_operator.py` (per spec Task 5 acceptance).
**Actual:** Zero matches for all three patterns.
**Steps to reproduce:**
```bash
grep -nE 'spec-verification-protocol|spec_verify|spec_operator' .claude/skills/qa/SKILL.md
# returns nothing
```
**User impact:** When `/qa` is invoked on a closed spec, the skill does not point operator at the verification protocol — they have to find it manually.
**Hint:** Append the section drafted in spec Task 5 (lines 263–283) above the footer.

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| 1 | `spec_verify.py` exists and `--help` works | Two positional args (`project`, `spec_id`) shown correctly. |
| 2 | `spec_operator.py` exists; subcommands listed | `demote`, `force-done`, `reset-circuit` all present in `--help`. EC-3 path. |
| 3 | EC-1 surrogate `test_spec_verify_reports_missing_file` | PASS |
| 4 | EC-2 surrogate `test_spec_verify_ok_when_files_and_symbols_present` | PASS |
| 5 | EC-3 `test_operator_demote_via_plumbing_does_not_touch_working_tree` + 2 sibling operator tests | PASS (3/3) |

## Verdict

Spec is **partially implemented** — code surface (Tasks 2/3/4) is green and matches EC-1/EC-2/EC-3. Documentation surface (Tasks 1 and 5) is missing, so EC-4 fails and the operator-facing protocol does not actually exist where it must auto-load. Spec must remain `queued` until Tasks 1 and 5 are completed.

## Fixes Applied

None — gaps are >5 LOC documentation work, out of QA light-fix scope.
