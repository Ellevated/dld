# QA Report: BUG-188 (claude-runner post-result Exception false-fail)

**Date:** 2026-05-20
**Environment:** local checkout `/home/dld/projects/dld` @ `develop` HEAD `bd5f045`
**Trigger:** `/qa BUG-188` — verify the closed spec actually delivered

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 10 + 6 acceptance checks | 15 | 0 | 0 |

Note: 2 issues found that are **NOT failures of the BUG-188 fix itself** — see
"Hygiene findings" below (dirty lifecycle YAML in WT, leftover test rows in
prod DB).

## Spec Status Reconciliation

- **HEAD (SoT, ADR-023):** `ai/lifecycle/BUG-188.yaml` — `status: done`,
  finalized by callback at `2026-05-20T10:29:04Z` via `pueue_id: 352`.
- **Lifecycle commits:** `90f85b8 lifecycle(BUG-188): queued` →
  `bd5f045 lifecycle(BUG-188): done` (two transitions, both via plumbing).
- **Implementation commits (6):** `5c011ad` (Task 1) → `9e88a2e` (Task 2) →
  `944b621` (Task 3) → `57c2ac5` (Task 4) → `93c8c9f` (Task 5) →
  `fd1019a` (Task 6) → merged via `96e7ef3`.
- **Working tree (LOCAL):** `ai/lifecycle/BUG-188.yaml` is **modified, reverting
  done → queued** (see Hygiene #1). HEAD wins per ADR-023; this is a local-WT
  artefact, not a status regression.

## Step 2 — File existence (9/9 OK)

All `## Allowed Files` paths exist with recent commits:

```
OK   scripts/vps/claude-runner.py                                    (9 commits)
OK   .claude/skills/autopilot/SKILL.md                               (4 commits)
OK   template/.claude/skills/autopilot/SKILL.md                      (3 commits)
OK   scripts/vps/schema.sql                                          (3 commits)
OK   scripts/vps/db.py                                               (5 commits)
OK   tests/integration/test_claude_runner_post_result_exception.py   (3 commits)
OK   tests/integration/test_sdk_post_result_errors_telemetry.py      (1 commit)
OK   .claude/rules/architecture.md                                   (10 commits)
OK   .claude/rules/dependencies.md                                   (8 commits)
```

## Step 4 — Tests (10/10 PASS, 5.29s)

```
tests/integration/test_claude_runner_post_result_exception.py
  ✓ test_post_result_exception_preserves_success         (regression for trace #269)
  ✓ test_pre_result_exception_marks_failure              (pre-result still fails correctly)
  ✓ test_result_message_is_error_true_marks_failure      (is_error=True still fails)
  ✓ test_timeout_exception_uses_exit_124                 (timeout branch intact)
  ✓ test_stderr_callback_captures_lines                  (Layer 2 stderr via public API)
  ✓ test_process_error_stderr_takes_precedence           (ProcessError.stderr wins)
  ✓ test_post_result_exception_logs_telemetry_row        (Layer 4 wiring)
tests/integration/test_sdk_post_result_errors_telemetry.py
  ✓ test_log_sdk_post_result_error_inserts_row
  ✓ test_table_auto_created_on_first_call                (no manual ALTER needed)
  ✓ test_stderr_column_nullable
```

## Step 6 — Acceptance Criteria (6/6 confirmed)

### Layer 1 — `result_received` / `result_is_error` tracking
- `scripts/vps/claude-runner.py:166-167` — flags initialised before `try`.
- `scripts/vps/claude-runner.py:194-195` — set inside `ResultMessage` branch.
- `scripts/vps/claude-runner.py:256-262` — except handler logs WARNING and
  preserves `exit_code=0` when `result_received and not result_is_error`.
- Warning literal `"SDK post-ResultMessage exception (work completed)"` present
  → can be grepped in `callback-debug.log` to count saved false-fails.

### Layer 2 — public stderr callback (no monkey-patch)
- `scripts/vps/claude-runner.py:120-125` — `stderr_lines` collector with 200-line cap.
- `scripts/vps/claude-runner.py:149` — `stderr=_stderr_collector` passed to
  `ClaudeAgentOptions` (public API, per planner drift note).
- No `_internal` import (verified via `grep _internal scripts/vps/claude-runner.py` → 0 hits).

### Layer 3 — autopilot SKILL.md early-exit
- `.claude/skills/autopilot/SKILL.md` — 3 hits on "Already-implemented detection" / "BUG-188".
- `template/.claude/skills/autopilot/SKILL.md` — same 3 hits.
- Step inserted into the "Pre-flight Check" section before Plan invocation.

### Layer 4 — telemetry table + helper
- `scripts/vps/schema.sql:91-103` — `CREATE TABLE IF NOT EXISTS sdk_post_result_errors` + index.
- `scripts/vps/db.py:63-79` — idempotent CREATE inside `_ensure_migrations()`
  (auto-applies on existing VPS instances — verified live below).
- `scripts/vps/db.py:324` — `log_sdk_post_result_error()` helper.
- `scripts/vps/claude-runner.py:58-60, 267-276` — lazy import + telemetry call
  inside post-result branch, guarded so failure of telemetry never breaks runner.

### Production DB — table live
```
sqlite> .schema sdk_post_result_errors
CREATE TABLE sdk_post_result_errors (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ts TEXT NOT NULL DEFAULT (...),
  project_id TEXT NOT NULL, task TEXT NOT NULL,
  turns INTEGER, cost_usd REAL,
  error_msg TEXT, stderr TEXT
);
CREATE INDEX idx_sdk_post_result_errors_ts ON sdk_post_result_errors(ts);
```
Auto-migration via `_ensure_migrations` worked — no manual ALTER ran.

### Docs (ADR + dependencies)
- `.claude/rules/architecture.md` — `grep -c "ADR-024"` = 1.
- `.claude/rules/dependencies.md` — `grep -c "BUG-188"` = 5 (claude-runner Uses,
  db Used by, Last Update, Layer 2/4 cross-refs).

## Hygiene findings (NOT regressions of BUG-188 fix)

### F1 — Dirty `ai/lifecycle/BUG-188.yaml` in working tree

**Severity:** Major
**Reproducibility:** Always (current local state)
**Expected:** Per ADR-023 "no dirty WT" invariant, `ai/lifecycle/*.yaml` must
match HEAD. If the orchestrator restarts with this WT, `assert_clean_lifecycle_tree()`
should abort startup.
**Actual:**
```
$ git diff HEAD -- ai/lifecycle/BUG-188.yaml
- finished_at: '2026-05-20T10:29:04Z'
+ finished_at: null
- pueue_id: 352
+ pueue_id: null
- status: done
+ status: queued
- transitions: [{at: ..., by: callback, from: queued, to: done, ...}]
+ transitions: []
- version: 2
+ version: 1
```
**User impact:** Orchestrator restart will refuse to start until WT is clean
(either `git checkout` the yaml or commit a deliberate change). Probably leftover
from a manual operator session — needs `git checkout -- ai/lifecycle/BUG-188.yaml`
before next orchestrator boot.
**Evidence:** `git status --short ai/lifecycle/BUG-188.yaml` → `M`.
**Hint:** Run `git checkout -- ai/lifecycle/BUG-188.yaml` (HEAD is the SoT).

### F2 — Test pollution in production `orchestrator.db`

**Severity:** Minor
**Reproducibility:** Historical (already happened; new runs are isolated)
**Expected:** `tests/integration/test_*.py` should not write to
`scripts/vps/orchestrator.db`.
**Actual:** 5 rows present in `sdk_post_result_errors` with
`project_id='test_post_result_exception_pre0'`, `task='TEST-001'`, dated
`2026-05-20T10:11:43Z` … `10:33:05Z` (during the autopilot dev session for
BUG-188 itself, before isolation was finalised).

Re-running `test_post_result_exception_logs_telemetry_row` now produces
**delta=0** new rows — current isolation via `monkeypatch.setenv("DB_PATH", ...)`
+ module reload works correctly.

**User impact:** Noise in telemetry queries; threshold-based alerts (>5/day)
would already be inflated by historical test data. Cleanup recommended.
**Evidence:**
```sql
SELECT COUNT(*) FROM sdk_post_result_errors
WHERE project_id LIKE 'test_post_result_exception_%';
-- 5
```
**Hint:** Once-off cleanup:
```sql
DELETE FROM sdk_post_result_errors
WHERE project_id LIKE 'test_%' OR task = 'TEST-001';
```

## Live false-fail signal (post-deploy)

`task_log` since callback finalised BUG-188 done (`2026-05-20T10:29:04Z`)
contains only the qa/reflect dispatches triggered by this verification.
No autopilot runs have occurred yet, so the regression cannot be observed in
production traffic — only synthetic regression in the integration suite.

The integration test `test_post_result_exception_preserves_success` already
captures the exact trace (`turns=43`, `cost=$6.32`, post-ResultMessage Exception
→ `exit_code=0`) — it is a deterministic re-creation of pueue task #269
from the spec's "Symptom" table.

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| 1 | post-result Exception preserves exit_code=0 | matches spec trace #265/#269/#262 |
| 2 | pre-result Exception still marks failure | exit_code=1 retained |
| 3 | ResultMessage(is_error=True) marks failure | genuine failure path intact |
| 4 | timeout Exception uses exit_code=124 | branch unchanged |
| 5 | stderr callback collects subprocess lines | Layer 2 via public API |
| 6 | ProcessError.stderr takes precedence | no duplication |
| 7 | telemetry row inserted in post-result branch | Layer 4 wiring |
| 8 | log_sdk_post_result_error inserts | helper smoke |
| 9 | table auto-created on first call | `_ensure_migrations` self-heals |
| 10 | stderr column nullable | schema sanity |
| A1 | Allowed-files existence | 9/9 OK |
| A2 | claude-runner.py changes | L1+L2+L4 verified by grep |
| A3 | db.py + schema.sql | table + helper present |
| A4 | autopilot SKILL.md × 2 | both files updated |
| A5 | ADR-024 + dependencies | docs present |
| A6 | production DB has table live | `.schema` output confirmed |

## Verdict

BUG-188 fix is **structurally complete and verified** — all 4 layers landed,
all 10 tests pass, production DB carries the telemetry table, docs reference
ADR-024. **No demote needed.**

Two hygiene findings (F1 dirty WT, F2 test-row pollution) are
unrelated to the bug being fixed and should be cleaned up by the operator
in a separate small follow-up.
