# QA Report: TECH-174 Manual Spec Verification Protocol

**Date:** 2026-05-04
**Environment:** Local repo `/home/dld/projects/dld` (CLI tooling — not a deployed product)
**Trigger:** `/qa TECH-174`
**Spec status at QA time:** `queued` (not yet `done`)

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 7     | 5    | 2    | 0       |

Tooling (spec_verify.py, spec_operator.py, integration tests) works as designed.
Two of the five spec tasks are not delivered yet — Task 1 (protocol doc) and Task 5 (qa skill ref).
Spec's own Drift Log (lines 122–123) already flagged both as "Plan: CREATE / ADD" — confirmed still open.

## Failures

### F1: Protocol document missing (Task 1)

**Severity:** Major
**Reproducibility:** Always
**Expected:** `~/.claude/projects/-root/memory/spec-verification-protocol.md` exists with `## Step 1`..`## Step 7` headings (EC-4).
**Actual:** File does not exist. Integration test `test_protocol_doc_has_seven_steps` SKIPPED for the same reason.
**Steps to reproduce:**
1. `ls ~/.claude/projects/-root/memory/spec-verification-protocol.md` -> not found
2. `python3 scripts/vps/spec_verify.py /home/dld/projects/dld TECH-174` -> reports `MISS ~/.claude/projects/-root/memory/spec-verification-protocol.md`
3. `pytest tests/integration/test_spec_verify.py::test_protocol_doc_has_seven_steps` -> SKIPPED

**Evidence:** spec_verify VERDICT: `HARD-FAIL — 2 missing file(s)`
**User impact:** Operator has no checklist to follow when auditing a suspicious `done` spec — the whole point of TECH-174.
**Hint for developers:** Spec lines 136–179 prescribe content (7 numbered headings + bash block + Step 7 must reference `spec_operator.py`, not stale `operator.py`).

### F2: `/qa` skill does not reference the protocol (Task 5)

**Severity:** Minor
**Reproducibility:** Always
**Expected:** `.claude/skills/qa/SKILL.md` contains references to `spec-verification-protocol.md`, `spec_verify.py`, `spec_operator.py` (Implementation Plan Task 5, spec lines 249–304).
**Actual:** Zero matches for all three.
**Steps to reproduce:**
```
grep -n 'spec-verification-protocol\|spec_verify.py\|spec_operator.py' .claude/skills/qa/SKILL.md
# (no output)
```
**Evidence:** spec_verify report: `[FAIL] Task 5: '.claude/skills/qa/SKILL.md': 0 hit(s)`
**User impact:** When operator triggers `/qa` against a closed spec, the skill won't redirect them to the verification checklist.
**Hint for developers:** Append the 25-line section from spec lines 263–283 above any Change History footer.

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| 1 | `spec_verify.py --help` runs and shows positional args | Task 2 acceptance OK |
| 2 | `spec_operator.py --help` lists 3 subcommands (demote, force-done, reset-circuit) | EC-3 surface OK |
| 3 | `pytest test_spec_verify.py` — 6 pass / 1 skipped | green except F1 skip |
| 4 | `spec_verify.py` self-run on TECH-174 produces correct HARD-FAIL verdict | EC-1 surrogate behaviour |
| 5 | `spec_operator.py demote --help` shows `--blocked` flag + positional args | Task 3 OK |

## Blocked

None.

## Notes

- EC-1/EC-2 reference external repo `awardybot` (not present on this VDS); surrogate tests in `test_spec_verify.py` cover the same logic and pass.
- Spec is still `queued`, so no callback false-done risk yet — this QA serves as pre-completion gate.
- After Tasks 1 + 5 land, re-run `python3 scripts/vps/spec_verify.py /home/dld/projects/dld TECH-174` — must print `VERDICT: OK`.

## Fixes Applied

None (gaps require >5 LOC of new content, out of light-fix scope).
