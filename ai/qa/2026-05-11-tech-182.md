# QA Report: TECH-182 — Orchestrator rebase --autostash data loss fix

**Date:** 2026-05-11
**Environment:** DLD framework repo `/home/dld/projects/dld`, dld-orchestrator systemd (active)
**Trigger:** `/qa TECH-182`

## Scope caveat

TECH-182 — инфраструктурный фикс (Python orchestrator/callback daemons), не user-facing продукт. `/qa` создан для UI/API/bot. Правильный инструмент для такого спека — `/tester` + spec-verification protocol. Провёл прагматичную проверку safety-контракта.

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 6     | 5    | 1    | 0       |

## Failures

### F1: Spec статус рассинхронизирован с git
**Severity:** Minor
**Reproducibility:** Always
**Expected:** `**Status:** done` — код смержен (`06baad6 Merge TECH-182`, `7003341 docs: mark TECH-182 as done`).
**Actual:** Спека держит `**Status:** queued | **Priority:** P1`.
**Steps to reproduce:**
1. `git log --oneline --grep="TECH-182"` — видно merge + "mark TECH-182 as done"
2. `grep "^\*\*Status:" ai/features/TECH-182*.md` → `queued`

**Evidence:**
```
06baad6 Merge TECH-182: orchestrator rebase autostash data-loss fix
7003341 docs: mark TECH-182 as done (callback auto-fix)
ae485e4 fix(TECH-182): clean spec — remove stale ACTION REQUIRED block, reset to queued
**Status:** queued | **Priority:** P1 | **Date:** 2026-05-10
```

**User impact:** Оркестратор `scan_inbox` (ADR-021) увидит `queued` и попытается передиспатчить уже выполненную работу → лишний autopilot цикл, потенциальный конфликт guard'а.
**Hint:** Коммит `ae485e4` сбросил статус в queued; последующий auto-done не закрепился. Закрыть вручную или через `python3 scripts/vps/spec_operator.py`.

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| 1 | Task 1 — `--autostash` удалён из `git_pull` | в `orchestrator.py` упоминание только в docstring/log message; `pull --ff-only origin develop` присутствует |
| 2 | Task 1 — `rebase --abort` удалён из except | `grep "rebase --abort" scripts/vps/orchestrator.py` → 0 hits |
| 3 | Task 2 — `_append_blocked_reason` без `write_text` | единственное упоминание `spec_path.write_text` — в docstring (объясняет миграцию). Функция использует `_read_head_blob` + `_git_commit_push` |
| 4 | Task 3 — `git push` failure логируется | `callback.py:531` `"STATUS_FIX: push failed for %s (rc=%d): %s"` через `log.warning` |
| 5 | Task 4 — Unit tests | `pytest tests/test_orchestrator_git_pull.py -v` → **5 passed** (dirty unstaged/staged skip, clean ff-only, no-git-dir, agent-running) |
| 6 | Daemon health | `systemctl is-active dld-orchestrator` → `active` |

## Recommendation

1. Закрыть спеку (статус → `done`) и запушить в develop.
2. Опционально: integration-тест end-to-end с реальным dirty working tree (текущие тесты mock'ают `subprocess.run`).
