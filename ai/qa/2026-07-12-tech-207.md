# QA Report: TECH-207 — QA/Reflect dispatch completion-signal robustness

**Date:** 2026-07-12
**Environment:** VPS orchestrator (callback.py, pueue, systemd dld-orchestrator.service)
**Trigger:** `/qa TECH-207` — verify merge-confirmed QA dispatch fallback works

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 6     | 5    | 0    | 1       |

## Blocked

### B1: Live merge-fallback trigger (S3)

**Reason:** Fix deployed 2026-07-12 16:33. Since then only 2 autopilot sessions completed (TECH-207, TECH-208) — both emitted `task_status=complete` normally, so the merge fallback path (`QA_DISPATCH_MERGE_FALLBACK`) was not exercised in production. This is expected: the fallback triggers only when background-notification hijack displaces the completion signal, which is probabilistic. Integration tests (EC-1) prove the fallback works with real git repos. Production verification requires waiting for a naturally-occurring displaced signal.

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| S1 | EC-1..EC-5 integration tests pass (7 tests) | All 7 tests pass in 1.74s. Real git repos via tmp_path (ADR-013). EC-1: merged + no complete -> dispatches. EC-2: no merge + no complete -> skips. EC-3: explicit complete unchanged. EC-4: blocked/needs_review skips. EC-5: dedup intact. |
| S2 | Existing callback tests (49) — no regressions | All 49 tests in test_callback.py pass (28.17s). Zero regressions from Step 6 extraction. |
| S4 | Post-fix done-autopilots have QA dispatched | 2/2 post-fix sessions (TECH-207 pueue=945, TECH-208 pueue=944) both have QA + Reflect tasks. All 5 "missing QA" sessions (BUG-1387, FTR-1394, BUG-1395, BUG-353, BUG-354) completed BEFORE fix deployment. |
| S5 | Failed sessions don't dispatch QA | Recent failed autopilots (pueue 943 plpilot:BUG-355, etc.) correctly have no QA tasks. |
| S6 | Full VPS test suite — 347/347 pass | Complete test suite passes in 64.19s. Includes callback, orchestrator, lifecycle, gate-logic, bootstrap, render-backlog tests. Zero failures. |

## Definition of Done Verification

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Done autopilot with merged work -> QA dispatched even when task_status != "complete" | Verified via EC-1 test | test_ec1_merged_no_complete_dispatches_qa_reflect PASSED |
| Done run with NO confirmed merge -> QA still skipped | Verified via EC-2 test | test_ec2_no_merge_no_complete_skips_dispatch PASSED |
| Explicit task_status=="complete" path unchanged | Verified via EC-3 test | test_ec3_explicit_complete_dispatches_normally PASSED |
| EC-1..EC-5 pass (real-repo, no mocks) | 7/7 pass | pytest output captured |
| Single-writer status model untouched | No lifecycle.py changes in diff | git log --oneline --all --grep="TECH-207" shows only callback.py + test file |
| Circuit-breaker (TECH-169) untouched | Confirmed | No changes to circuit-breaker logic |
| QA_DISPATCH_MERGE_FALLBACK log marker present | Confirmed in test + impl | EC-1 exercises this path |

## Fixes Applied

(none)

## Notes

- The TECH-207 autopilot itself completed with `task_status: "complete"` ($14.92, 2 turns), so this QA session was dispatched via the standard explicit_complete path.
- The merge fallback is a safety net for the background-notification hijack pattern. It will be exercised in production when the next session naturally displaces the completion signal. The `QA_DISPATCH_MERGE_FALLBACK` log marker provides observability for when this occurs.
- Historical: 5 pre-fix sessions lacked QA dispatch, consistent with the bug description (3/6 awardybot sessions skipped QA on 2026-06-28).
