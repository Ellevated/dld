# QA Report: TECH-183 — Spark Verified References

**Date:** 2026-05-14
**Environment:** repo `/home/dld/projects/dld` (HEAD `888a57a`)
**Trigger:** `/qa TECH-183` — verify spec was actually delivered.

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 7     | 6    | 1    | 0       |

Content-wise spec is fully implemented and template↔.claude copies match.
Status, однако, был демоушен с `done` → `queued` в working tree
(uncommitted) — классический паттерн false-demote callback'а (см. memory
`callback false-done-pattern`).

## Eval Criteria Results

| ID | Result | Evidence |
|----|--------|----------|
| EC-1 | ✅ PASS | `template/.claude/agents/spark/codebase.md:167` `## Verified References` секция с форматной таблицей + 4 примера-строки + Rules |
| EC-2 | ✅ PASS | `template/.claude/agents/spark/codebase.md:364` Rule #1 «Grep-evidence required» присутствует; старый текст «Grep first — no assumptions» отсутствует (grep = 0) |
| EC-3 | ✅ PASS | `template/.claude/skills/spark/feature-mode.md:802` `### Gate 8: Verified References` |
| EC-4 | ✅ PASS | `feature-mode.md:720` «8 structural validation gates»; «7 validation gates» отсутствует (grep = 0) |
| EC-5 | ✅ PASS | `feature-mode.md:219` Phase 2 exit-criteria требует `## Verified References` в research-codebase.md |
| EC-6 | ✅ PASS | `diff template/.claude/agents/spark/codebase.md .claude/agents/spark/codebase.md` → identical; то же для feature-mode.md |
| EC-7 | ⏭ N/A | Test corpus для spec-валидации не существует (spec помечает как conditional) |

## Failures

### F1: Спецификация и backlog демоушнуты с `done` → `queued` (uncommitted)

**Severity:** Major
**Reproducibility:** Always (на момент QA)
**Expected:** После merge commit `888a57a "docs: mark TECH-183 as done"`
статус остаётся `done` в обоих файлах.
**Actual:** `git status` показывает uncommitted изменения, откатывающие
`done` → `queued` в `ai/backlog.md:148` и `ai/features/TECH-183-…md:2`.
Контент имплементации (codebase.md, feature-mode.md) при этом полностью
на месте — это типичный false-demote callback guard'а (TECH-176/177/179
покрывали 3 разных корня, но рецидив).

**Steps to reproduce:**
1. `git log --oneline -3 ai/backlog.md` → видно `888a57a` помечает done.
2. `git status -s ai/backlog.md ai/features/TECH-183*.md` → ` M ` обе.
3. `git diff` → строки `done` → `queued`.

**Evidence:** `git diff` вывод сохранён в этом отчёте (см. выше commit
888a57a и текущее состояние working tree).
**User impact:** Orchestrator scan_inbox увидит спеку как `queued` и
снова отправит её в autopilot — wasted compute slot + риск двойной
имплементации поверх уже выполненной работы.
**Hint for developers:** Откатить uncommitted demote — `git checkout --
ai/backlog.md ai/features/TECH-183-2026-05-14-spark-verified-references.md`
— и сверить с callback-guard audit log (`scripts/vps/callback.py`,
`_spec_has_merged_implementation`), почему гард не увидел коммит
`9033ac4 feat(spark): TECH-183 verified references gate`.

## Passed (content)

| # | Scenario | Notes |
|---|----------|-------|
| 1 | `## Verified References` секция в codebase.md | формат + 4 примера + Rules + ссылка на Gate 8 |
| 2 | Rule #1 переписан на grep-evidence-required | старая формулировка вычищена |
| 3 | Gate 8 в Phase 6 | + soft sub-rule для degraded codebase scout |
| 4 | HARD-GATE счётчик 7 → 8 | «All 8 validation gates pass» |
| 5 | Phase 2 exit-criteria | требует `## Verified References` |
| 6 | template/ ↔ .claude/ sync | diff = 0 для обоих файлов |

## Fixes Applied

Нет. False-demote — это не light-fix QA, а инфраструктурный bug callback'а.
Reporting only; не трогаю git state, чтобы операторы могли увидеть рецидив
вживую и решить, нужен ли новый TECH (callback false-demote round 4).
