# QA Report: TECH-151 — Orchestrator North-Star Alignment

**Date:** 2026-03-17
**Environment:** VPS / orchestrator scripts / skill docs
**Trigger:** Проверь изменения после TECH-151

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 10    | 3    | 6    | 1       |

**Вердикт:** TECH-151 НЕ реализован. Спека создана (27d6900), но ни одна из 5 задач не была закоммичена. Коммит 3a27f82 (до спеки) частично менял файлы, но не завершил работу. Все 6 основных eval criteria — FAIL.

## Failures

### F1: Spark SKILL.md всё ещё использует `draft` как target status (EC-1)

**Severity:** Major
**Reproducibility:** Always
**Expected:** Spark SKILL.md указывает `queued` как output status
**Actual:** Строки 59, 63 всё ещё содержат "Create spec in `draft` status"
**Steps to reproduce:**
1. `grep -n "draft" .claude/skills/spark/SKILL.md`
2. Наблюдаем 2 строки с draft как target
**User impact:** Spark-агент создаёт спеки в draft вместо queued → orchestrator не подхватывает их автоматически

### F2: completion.md полон draft-ссылок (EC-2)

**Severity:** Major
**Reproducibility:** Always
**Expected:** completion.md ссылается на `queued` как выходной статус Spark
**Actual:** 10+ строк с draft: строки 45, 82, 83, 93, 99, 100, 101, 155-157
**Steps to reproduce:**
1. `grep -n "draft" .claude/skills/spark/completion.md`
2. Наблюдаем массу draft-ссылок
**User impact:** Чеклист завершения Spark инструктирует агент ставить draft → блокирует автоматический пайплайн

### F3: bug-mode.md всё ещё draft (EC-2 расширение)

**Severity:** Major
**Reproducibility:** Always
**Expected:** bug-mode.md указывает `queued`
**Actual:** Строки 197-198 (`status: draft` в чеклисте), строка 211 (`spec_status: draft`)
**User impact:** Bug-спеки создаются как draft → не попадают в автопайплайн

### F4: SENT_APPROVAL / Step 5.5 не удалён из callback (EC-4)

**Severity:** Major
**Reproducibility:** Always
**Expected:** Step 5.5 (draft approval) удалён из pueue-callback.sh
**Actual:** SENT_APPROVAL на строках 212, 298, 345, 450; notified-drafts на строках 236, 240; spec_approve на 247
**Steps to reproduce:**
1. `grep -n "SENT_APPROVAL" scripts/vps/pueue-callback.sh`
2. 5 совпадений
**User impact:** Callback шлёт approval-уведомления для несуществующего draft-flow → лишний шум в Telegram

### F5: WILL_WRITE_INBOX не удалён из callback (EC-5)

**Severity:** Minor
**Reproducibility:** Always
**Expected:** WILL_WRITE_INBOX логика удалена из pueue-callback.sh
**Actual:** WILL_WRITE_INBOX на строках 276, 279, 285
**User impact:** Мёртвый код, не влияет на поведение напрямую, но сбивает при чтении

### F6: Reflect всё ещё пишет в inbox (EC-6)

**Severity:** Major
**Reproducibility:** Always
**Expected:** Reflect SKILL.md инструктирует писать только в diary
**Actual:** 14 ссылок на inbox в reflect/SKILL.md (строки 108, 113, 130-133, 139-140, 171, 181-182, 184, 195, 207, 211-212)
**User impact:** Reflect-агент создаёт ai/inbox/ файлы → inbox-processor их подхватывает → спавнит ненужные Spark-задачи → spam

## Blocked

### B1: End-to-end pipeline test

**Reason:** systemd сервисы dld-orchestrator и dld-telegram-bot неактивны. Невозможно протестировать живой pipeline (inbox → spark → backlog → autopilot → callback → qa/reflect).

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| 1 | EC-3: scan_drafts удалён из orchestrator.sh | `grep scan_drafts` = 0 результатов ✓ |
| 2 | EC-8: Template sync | Все 4 spark файла + reflect SKILL.md синхронизированы (diff = 0) ✓ |
| 3 | Bash syntax validation | `bash -n orchestrator.sh` и `bash -n pueue-callback.sh` — OK ✓ |

## Дополнительные находки

### D1: CLAUDE.md Task Statuses не обновлён

**Severity:** Minor
**Expected:** draft описан как "manual/incomplete only", queued как Spark output
**Actual:** `draft | Spark | Spec incomplete` — подразумевает что Spark нормально создаёт draft

### D2: feature-mode.md строка 643

**Severity:** Cosmetic
**Actual:** `spec stays draft` при gate failure — это единственное допустимое использование draft (спека это разрешает)
**Verdict:** PASS — это gate failure fallback, не target status

### D3: pytest не установлен в venv

**Severity:** Minor
**Expected:** `python3 -m pytest tests/` работает
**Actual:** `No module named pytest` — venv пересоздан (66247d9), но pytest не установлен
**User impact:** Нельзя запустить тесты оркестратора
