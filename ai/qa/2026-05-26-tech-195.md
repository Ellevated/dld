# QA Report: TECH-195 — Orchestrator bootstrap fragility (default-to-done)

**Date:** 2026-05-26
**Environment:** /home/dld/projects/dld @ `839aa69` (develop) + orchestrator `active`
**Trigger:** `/qa TECH-195`
**Type:** Spec verification (closed spec, status=done)

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 13    | 13   | 0    | 0       |

**Verdict:** ✅ Спека реально доставлена. Все 5 Tasks подтверждены кодом, тестами и работающими CLI-инструментами. Эфемерных «бутстрапнутых-в-done» спек в момент проверки — 944 штук в 9 проектах, инструмент аудита это видит, операторская очистка остаётся за человеком (как и сказано в спеке).

---

## CI + Deploy gate (Phase 0)

| Check | Result |
|-------|--------|
| `git rev-parse HEAD` | `839aa69` |
| Lifecycle `TECH-195.yaml` status | `done`, transition `queued→done` by `callback` 2026-05-26T10:15:11Z, pueue_id 466 |
| Orchestrator service | `active` (heartbeat updated 13:13 May 26) |
| 5 commit cluster | `df1bdb6` parser → `b6c787e` recovery → `7350f5d` audit → `74268ac` docs → `2229541` lifecycle done ✓ |

---

## Spec verification (Steps 1-3 via `spec_verify.py`)

| File (Allowed) | Exists | Recent commits |
|---|---|---|
| `scripts/vps/orchestrator.py` | ✅ | 21 |
| `scripts/vps/lifecycle.py` | ✅ | 13 |
| `scripts/vps/lifecycle_audit.py` (NEW, 525 LOC) | ✅ | 1 |
| `scripts/vps/recover_bootstrap_as_done.py` (NEW, 261 LOC) | ✅ | 1 |
| `scripts/vps/tests/test_orchestrator_bootstrap.py` (645 LOC) | ✅ | 3 |
| `.claude/rules/architecture.md` (ADR-026) | ✅ | 13 |
| `.claude/rules/dependencies.md` | ✅ | 15 |

Heuristic `spec_verify.py` отметил 2 «FAIL» (`status = None`, `pueue_id != null`) — это литералы из текста спеки, а не код-символы; семантически проверки выполнены (`t.get('pueue_id') is None` и т.п.).

---

## Eval Criteria

### Deterministic

| ID | Check | Result |
|---|---|---|
| D1a | `_parse_backlog` на short формате `\| ID \| status \| kind \|` → `queued` | ✅ возвращает `{'TECH-1082': 'queued'}` |
| D1b | `_parse_backlog` на template формате `\| ID \| desc \| status \| pri \|` → `queued` | ✅ возвращает `{'TECH-200': 'queued'}` |
| D1c | `_parse_backlog` на мусоре → `None` (не `done`) | ✅ возвращает `{'TECH-999': None}` |
| D2 | `pytest scripts/vps/tests/test_orchestrator_bootstrap.py` | ✅ **39/39 passed** в 2.69s |
| D3 | `.bootstrap-unparsable-count` counter существует в коде (создаётся при fallback) | ✅ 11 hits в `orchestrap.py` + `lifecycle_audit.py`; файл ещё не появился на проде, потому что новый parser справляется с обоими форматами |
| D4 | `recover_bootstrap_as_done.py` (dry-run default) на awardybot | ✅ 78 кандидатов, exit 0, ни одного `git write` |
| D5a | `lifecycle_audit.py --category=bootstrap_as_done --json` | ✅ валидный JSON, **944 findings в 9 проектах** (awardybot 78, dld 168, dowry 60, dowry-mc 83, nexus 20, plpilot 220, gipotenuza 107, memyselfandi 28, wb 180) |
| D5b | Audit exit code когда findings есть | ✅ exit code `1` |
| D5c | Audit exit code на --category=git_divergence (clean DLD) — sanity | ✅ работает per-category |

### Integration

| ID | Check | Result |
|---|---|---|
| I1 | Real-git тест awardybot-style backlog → yaml=queued | ✅ `test_bootstrap_short_format_awardybot_style` PASS |
| I2 | Recovery `--confirm` демотит через `recover_bootstrap_artifact` | ✅ `test_recover_confirm_demotes_via_recovery_primitive` PASS |
| I3 | `recover_bootstrap_artifact` отказывается демотить легитимный `done` | ✅ `test_recover_bootstrap_artifact_refuses_legitimate_done` PASS |

### Regression (Rule 7 / SoT / bootstrap idempotency)

| ID | Check | Result |
|---|---|---|
| R1 | Bootstrap идемпотентен (не перезаписывает существующий yaml) | ✅ `test_bootstrap_idempotent_after_refactor` PASS |
| R2 | Spark с template-format backlog | ✅ `test_bootstrap_template_format_still_works` PASS |

---

## Docs / Dependency map (Task 5)

| Artefact | Status |
|---|---|
| ADR-026 в `.claude/rules/architecture.md` | ✅ Присутствует — полное описание parser safety contract + recovery primitive + audit tool |
| `dependencies.md` секции для `lifecycle_audit.py` + `recover_bootstrap_as_done.py` | ✅ Обе секции + reverse-pointers в `lifecycle.py` |
| `dependencies.md` Last Update строка | ✅ 2026-05-26 TECH-195 запись (39 file tests / 212 total) |
| `template/.claude/rules/architecture.md` ADR-026 sync | ⚠️ Не синхронизировано — это **корректно** per spec («если применимо, т.к. orchestrator — DLD-only») |

---

## Observed but NOT a delivery defect

### O1: awardybot TECH-1082 / BUG-1074 still in HEAD as bootstrap-as-done

DoD упоминал «TECH-1082 и BUG-1074 разморожены». Сейчас:
- `awardybot/ai/lifecycle/TECH-1082.yaml` HEAD: `status=done, transitions=[], pueue_id=null` — **классическая bootstrap-as-done сигнатура**
- `awardybot/ai/lifecycle/BUG-1074.yaml` HEAD: `status=done` **с** transitions (queued→done) и pueue_id 421 — легитимный done, как и описано в S2 спеки («yaml был руками доведён до done через имитацию callback'а»)

Спека явно отделяет это от своей зоны: «**Не запускай recovery script на проде из спеки** — это Task 2 deliverable, оператор сам решит когда запустить». Recovery — runbook action для оператора, не для autopilot.

### O2: awardybot WT messy

```
M  ai/lifecycle/BUG-1081.yaml
M  ai/lifecycle/TECH-1073.yaml
D  ai/lifecycle/TECH-1082.yaml
?? ai/lifecycle/TECH-1082.yaml
```

— это в точности тот мусор, который описан в S1 («файл болтается как `?? ai/lifecycle/TECH-1082.yaml`»). Чистится `scripts/vps/cleanup-lifecycle-drift.sh` (TECH-194). Не относится к TECH-195.

### O3: масштаб bootstrap-as-done пострашнее ожидаемого

Спека ожидала «не единичный случай в awardybot/dowry» (Task 3 hypothesis). Реальность:
- **944 кандидата на demote через 9 проектов**
- Самые тяжёлые: plpilot 220, wb 180, dld self 168, gipotenuza 107
- Это backlog исторических данных, накопленный до TECH-195. Новый parser защищает от создания **новых** bootstrap-as-done; старые ждут операторской команды `recover_bootstrap_as_done.py --confirm`.

Не делаю фикса/демота автоматически — оператору решать, какие из этих 944 реально были «historical archive» (легитимный исходный замысел `default-to-done`) vs. потерянные спеки.

---

## Light fixes

Никаких. Спека чистая, артефакты на местах, тесты зелёные.

---

## Recommended next steps for operator (НЕ часть TECH-195 delivery)

1. `cd /home/dld/projects/awardybot && bash /home/dld/projects/dld/scripts/vps/cleanup-lifecycle-drift.sh awardybot` — почистить S1 orphan (TECH-194 helper).
2. `python3 scripts/vps/recover_bootstrap_as_done.py --project=awardybot` — глазами просмотреть список 78 кандидатов.
3. Решить policy: demot'ить ли все 944 одним махом, или фильтровать (например — только последние N дней).
4. После выборочного `--confirm` запустить `lifecycle_audit.py --quiet` для подтверждения 0 findings.

---

## Passed

| # | Scenario | Notes |
|---|----------|-------|
| 1 | All 7 Allowed Files exist + recently committed | spec_verify Step 2 |
| 2 | ADR-026 присутствует в architecture.md root | grep подтвердил полный текст |
| 3 | `_parse_backlog` short awardybot format → `queued` | D1a |
| 4 | `_parse_backlog` template format → `queued` | D1b |
| 5 | `_parse_backlog` garbage → `None` (safe default to queued in caller) | D1c |
| 6 | 39/39 pytest unit + integration + regression | D2 |
| 7 | `.bootstrap-unparsable-count` counter wired | D3 (code path) |
| 8 | recover script `--dry-run` lists candidates on awardybot | D4 (78 found) |
| 9 | `lifecycle_audit.py --json` returns valid JSON, exit 1 with findings | D5 |
| 10 | dependencies.md has both new modules + reverse pointers | Task 5 |
| 11 | Orchestrator service still alive post-deploy | active, heartbeat 13:13 |
| 12 | Lifecycle write-once-done holds (TECH-195.yaml signed by callback only) | by=callback, version=2 |
| 13 | No staged `ai/lifecycle/*.yaml` in DLD WT after spec close | clean |
