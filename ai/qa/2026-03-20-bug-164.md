# QA Report: BUG-164 — callback.py pueue socket mismatch

**Date:** 2026-03-20
**Environment:** DLD Orchestrator (VDS), pueue + systemd + SQLite
**Trigger:** BUG-164 fix deployed — verify callback correctly resolves labels and dispatches QA/Reflect

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 8     | 7    | 0    | 1       |

## Blocked

### B1: Cross-project callback (awardybot)

**Reason:** Task awardybot:BUG-710 (pueue #749) completed at 10:13, BEFORE the BUG-164 fix was deployed (fix landed via task #752 at 10:35). Cannot verify cross-project behavior post-fix. Reflect did dispatch (task #750) but QA did not — consistent with the old broken behavior described in the bug.

**Retest:** Next autopilot task for awardybot will confirm cross-project fix.

## Passed

| # | Scenario | Evidence |
|---|----------|----------|
| 1 | Callback resolves label from DB (task_log WHERE pueue_id=752) | `project_id=dld, task_label=dld:BUG-164, skill=autopilot` |
| 2 | Agent output extracted from log file (not pueue CLI) | `dld-20260320-103055.log`: `skill=autopilot, preview_len=360` |
| 3 | QA dispatched after autopilot task | Task #753 `dld:qa-BUG-164` started 10:36:05, 8 seconds after task #752 completed |
| 4 | Reflect dispatched after autopilot task | Task #754 `dld:reflect-dld:BUG-164` completed successfully (10:36-10:37) |
| 5 | Log files contain valid JSON with required fields | 5/5 recent log files pass: `exit_code`, `project`, `skill`, `result_preview` all present |
| 6 | DB task_log updated with exit_code and finished_at | 5/5 done tasks have `exit_code=0` and `finished_at` populated |
| 7 | No QA recursion post-fix | 0 `qa-qa-*` tasks after March 20 (5 existed from March 12 pre-fix) |
| 8 | No QA dispatch for non-autopilot tasks (spark) | Task #751 `dld:inbox-20260320-082534` (spark) completed — no QA task followed |

## Observations (not bugs, informational)

### O1: output_summary always NULL

All 14 task_log rows have `output_summary = NULL`. The field exists in schema but callback doesn't populate it. If this field was intended to store `result_preview` from log files, it's not being written.

**User impact:** None (field not used downstream). May be a missed opportunity for quick task result lookup without reading log files.

### O2: Old QA recursion chains (pre-fix, March 12)

5 `qa-qa-*` recursive task chains exist from March 12 (plpilot project). Example chain: `qa-inbox-* -> qa-qa-inbox-* -> qa-qa-qa-inbox-*`. No recursion observed post-fix. This suggests the fix correctly prevents recursive QA dispatch.

### O3: Before/After comparison

| Metric | Before fix | After fix |
|--------|-----------|-----------|
| `skill` in callback | `""` (empty) | `"autopilot"` (correct) |
| `preview_len` in callback | `0` | `360` (correct) |
| QA dispatch | Never fired | Fires correctly for autopilot tasks |
| Reflect dispatch | Inconsistent | Fires correctly |
| QA recursion | Yes (5 chains) | No recursion |
