# QA Report: ARCH-186 — Recheck #2 (5-минутный delta)

**Date:** 2026-05-16 (recheck-2 at 10:40Z, через 6 мин после recheck #1)
**Environment:** /home/dld/projects/dld (develop @ `a0c5287`, branch `arch/ARCH-186` @ `7e32f3d`)
**Trigger:** `/qa ARCH-186` — повторный прогон сразу после `2026-05-16-arch-186-recheck.md` (10:34Z).
**Mode:** Spec-Verification Protocol, минимальный delta-check.

---

## Summary

| Total | Pass | Fail | Blocked |
|-------|------|------|---------|
| 4     | 0    | 3    | 1       |

**Verdict: HARD-FAIL — состояние идентично recheck #1 + одно новое подтверждение, что баг продолжает срабатывать сам по себе.**

Это **5-й рецидив** `callback false-done pattern` (см. project memory `callback-false-done-pattern.md`).
Полный анализ — в `2026-05-16-arch-186-recheck.md`. Здесь — только что изменилось.

---

## Что изменилось за 6 минут с recheck #1

| Аспект | Состояние |
|---|---|
| `develop` HEAD | `a0c5287` — **не сдвинулся** |
| `arch/ARCH-186` HEAD | `7e32f3d` — **не сдвинулся** |
| Реализация на develop | 6 файлов из Allowed Files по-прежнему MISS |
| Working tree | те же 3 файла unstaged (`Status: queued` revert не закоммичен) |
| `dld-orchestrator.service` | **active (running)** PID 169780, uptime 1d 13h |
| `callback_decisions` | **+1 новый эпизод** (id 1000/1001 в 10:39:38Z) |

---

## Failures

### F1: Callback продолжает срабатывать на автопилоте (новое свидетельство)

**Severity:** Critical
**Reproducibility:** Постоянная — fires само, без действий founder'а.

```
id    ts                    verdict  reason                       demoted
1001  2026-05-16T10:39:38Z  sync     spec_already_done            0   ← НОВЫЙ, после recheck #1
1000  2026-05-16T10:39:38Z  demote   no_implementation_commits    1   ← НОВЫЙ, после recheck #1
 999  2026-05-16T10:32:41Z  sync     spec_already_done            0
 998  2026-05-16T10:32:41Z  demote   no_implementation_commits    1
 997  2026-05-16T10:26:26Z  sync     spec_already_done            0
 996  2026-05-16T10:26:26Z  demote   no_implementation_commits    1
```

Каденция: ~6 минут между циклами (08:42, 08:48, 10:23, 10:26, 10:32, 10:39). Это значит, что
к моменту прочтения этого отчёта callback уже сделает следующий пробег.

**User impact:** Орchestrator физически не даёт удержать спек в `queued` без отключения сервиса
или хотфикса guard'а. Founder'у не успеть закоммитить ручной revert — следующий cycle перезапишет.

---

### F2: Все Failures из recheck #1 остаются в силе

- F1 recheck #1: **6 missing files / 3 stale-existing** — без изменений
- F2 recheck #1: **double-check race demote→sync_already_done** — подтверждено новым эпизодом (id 1000/1001)
- F3 recheck #1: **ACTION REQUIRED игнорируется guard'ом** — без изменений

Детали — в `2026-05-16-arch-186-recheck.md`, повторять смысла нет.

---

### F3: Working-tree revert не закоммичен → следующий cycle снова перезапишет

**Severity:** Major
**Reproducibility:** Always (математически — пока WT не закоммичен, состояние HEAD = `done`)

```
$ git status --porcelain
 M ai/backlog.md
 M ai/diary/.last_reflect
 M ai/features/ARCH-186-2026-05-16-orchestrator-lifecycle-file-sot.md
```

Founder вручную в working-tree поменял `Status: done → queued` в обоих файлах, но коммита нет.
Guard читает HEAD (где `done`), видит «уже done» → `sync_already_done`. Revert не имеет силы
до коммита.

**User impact:** Видимое состояние («ну вот же queued в файле») расходится с тем, что видит
система. Иллюзия контроля.

---

## Blocked

### B1: lifecycle.py unit tests — не запускал

Тот же бордер, что в recheck #1. Запуск pytest = `/tester`, не `/qa`.

---

## Passed

Никаких.

---

## Fixes Applied

| # | What | File | Commit |
|---|------|------|--------|
| — | — | — | — |

Light-fix остаётся неприменим (см. recheck #1).

---

## Immediate Action Required (или баг сейчас выстрелит снова)

**Окно ~5 минут до следующего callback cycle.** Чтобы остановить кровь:

1. **Stop the bleed** — один из двух вариантов, на выбор:
   ```bash
   # Вариант A: остановить orchestrator
   systemctl --user stop dld-orchestrator
   ```
   ```bash
   # Вариант B: официальный demote через operator-CLI (закоммитит сам)
   python3 scripts/vps/spec_operator.py demote /home/dld/projects/dld ARCH-186 \
     "false-done loop: 5 demote+sync_already_done cycles за 2h. ARCH-186 реализация на arch/ARCH-186 ветке (Tasks 1-3), не на develop. Tasks 4-11 не сделаны."
   ```

2. **Закоммитить или сбросить WT** — текущий `git status -s` показывает 3 модифицированных файла
   unstaged. Если revert намеренный — закоммить. Если случайный — `git checkout --` (но тогда
   спек снова на develop = `done`, и WT revert ничего не даст).

3. **Hot-fix guard** (или ускорить ARCH-186 till Tasks 4-11) — см. recheck #1 пункт 3.

4. **Решение по `arch/ARCH-186` branch** — см. recheck #1 пункт 4.

---

## Что я НЕ делал (HARD BOUNDARIES per /qa skill)

- ❌ Не запускал `spec_operator.py demote` сам — это операторское действие, requires founder
  явного "да". Решение — за founder'ом.
- ❌ Не останавливал `dld-orchestrator.service` — то же самое: оператор решает.
- ❌ Не читал `callback.py`/`orchestrator.py` сорцы.
- ❌ Не запускал pytest.

Действия выше — только git status/log + `sqlite3` на operator-DB + `spec_verify.py`.
