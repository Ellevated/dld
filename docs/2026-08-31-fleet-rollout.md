# Раскатка скилл-сета на флот — что сделано и что осталось решить

**31.08.2026.** Четыре пункта: три блокера раскатки из аудита 30.08 плюс измерение,
которого не существовало. Ниже — что изменилось, что это дало в числах и какой
единственный вопрос остался за Олегом.

---

## Коротко

| | Было | Стало |
|---|---|---|
| Гейт-скрипты в 7 репо | `validate-allowlist.mjs` отсутствовал во всех 7 | доставлен; **и ещё 30 файлов**, которых ручной аудит не увидел |
| Цитата в шапке гейта | `callback.py:451-459` — за концом файла, чужой модуль | `gate_logic.py::_parse_allowed_files_v1`, закрыта тестом |
| GROWTH в промптах | нет ни в одном из 7 → ветка уходит в `task/`, прогон сгорает второй раз | 14 таблиц + 7 bash-`case` согласованы с `_BRANCH_PREFIX` |
| «Проект отстал на сколько?» | ответа не существовало, мерялось глазами | `scripts/check-fleet-drift.py`, число за 2 секунды |

---

## Что дало измерение

Первый машинный замер, 138 синхронизируемых файлов:

```
  Dowry-mc     identical=39   differs=99   absent=0
  wb           identical=43   differs=95   absent=0
  Dowry        identical=44   differs=94   absent=0
  gipotenuza   identical=44   differs=94   absent=0
  PLPilot      identical=44   differs=94   absent=0
  AwardyBot    identical=46   differs=92   absent=0
  MishkinLyap  identical=47   differs=91   absent=0
```

`absent=0` — результат сегодняшней раскатки: **91 файл создан, ноль перезаписан.**

**Ручной аудит назвал один отсутствующий скрипт. Машина нашла 31 файл**, и четыре из них
отсутствовали у всех семи сразу:

- `.claude/scripts/validate-allowlist.mjs` — известный блокер
- `.claude/scripts/check-prompt-integrity.mjs` — аудит пропустил
- `.claude/scripts/lib/capture-artifacts.mjs` — аудит пропустил
- `.claude/agents/spark/research.md` — **скаут, которого `/spark` диспатчит**

Это и есть аргумент за сторожа: та же работа руками, за минуту, без пропусков.

---

## Почему раскатка сделана наполовину — и это правильно

Перед перезаписью я посмотрел, чем именно отличаются файлы. Расхождения оказались
**не «отстали»**:

- у AwardyBot **364 собственных коммита** в `.claude/` за три месяца;
- `board/cfo.md` там multi-round (`board-agenda-R{N}.md`), в template такого нет;
- `feature-mode.md` у даунстримов на **4 скаутах** (`external` + `patterns`), template — на **3**
  (они слиты в `research`).

Слепой `--apply` затёр бы чужую работу — ровно то, за что 25.05 удалили `upgrade.mjs`.
Поэтому раскатка разделена на два акта:

- **Часть A, сделана.** `--apply-absent` — доставляет только то, чего нет. Создание файла
  ничего не разрушает, а закрывает громкий отказ: промпт зовёт скрипт, скрипта нет, `exit 127`
  на каждой спеке. Проверено: `node .claude/scripts/validate-allowlist.mjs` теперь отвечает
  `exit 2` (usage) вместо 127, а на реальной спеке AwardyBot — валидным JSON с кодами ошибок.
- **Часть B, сделана следом** (ниже). Перезапись расходящихся файлов — но не оптом, а
  только тех, про которые git-история template доказывает, что там нет ничего своего.

Маркер версии при частичной раскатке **намеренно не пишется** — проект не на этой версии,
и маркер, утверждающий обратное, был бы ровно той ложью, против которой всё это строилось.

---

## Маркер, который не может соврать

`.claude/DLD_VERSION` пишется только скриптом и хранит digest файлового набора на момент
доставки. При каждом запуске digest **пересчитывается с диска**: разошлось — `STALE`.

Это ответ на вопрос «почему в этот раз сработает». Репозиторий уже трижды обжигался на
маркере, которому верили вместо проверки:

- `index_status.head_sha` читается из git вживую и равен HEAD, даже если граф месячной давности;
- `file.py:LINE` в доках — одиннадцать цитат протухли молча;
- `exit 124` не писался в `task_log`, и 32 % таймаутов были невидимы.

Здесь «синхронизирован» — измерение, а не утверждение. Подделать маркер руками не выйдет:
тест `test_hand_written_marker_does_not_buy_a_clean_report` проверяет именно это.

---

## Второй тихий дефект: сторож не видел сам себя

Шапка `validate-allowlist.mjs` ссылалась на `callback.py:451-459`. Файл — 371 строка,
а парсер вообще уехал в `gate_logic.py` (в `callback.py` остался только алиас). Цитата
протухла дважды — и по строке, и по модулю.

Тест против таких цитат существовал с 30.08, но глобил только `docs/orchestrator/*.md` —
`.claude/scripts/**` он не видел. **Гейт против дрейфа не покрывал собственный гейт.**
Теперь покрывает, включая голую форму без бэктиков (протухшая цитата была именно такой,
в `//`-комментарии).

Промпты (`agents/**`, `skills/**`) намеренно оставлены вне: там 35 строчных цитат —
легитимные иллюстрации формата, указывающие на код даунстрима.

---

## Часть B — обновление снимков (сделана в тот же день)

Вопрос «обновлять файл или он держит специфику проекта» перестал быть вопросом вкуса.
`template_history` собирает digest **каждой версии, которую файл имел в истории git**.
Совпало — чистый снимок, никто его не трогал после копирования, обновление ничего не
разрушает. Не совпало — содержимое пришло не из template, решает человек.

Замер: из 805 расхождений **720 (89%) оказались чистыми снимками** января–августа,
85 держали своё. `--apply-clean` обновил первые и не тронул вторые.

| Проект | differs было | стало |
|---|---:|---:|
| AwardyBot | 92 | **42** |
| Dowry | 94 | **11** |
| Dowry-mc | 99 | **12** |
| gipotenuza | 94 | **7** |
| PLPilot | 94 | **6** |
| wb | 95 | **5** |
| MishkinLyap | 91 | **2** |

**Уцелело именно то, ради чего вся осторожность:**

- **AwardyBot** — guard в `session-end.mjs` против потери worktree автопилота (три
  прогона в ночь 21.08, $58.80 сделанной работы), гейты `check_domain_imports` /
  `check_docs_sync` под его слои `bots/`+`agents/`, нормализатор доменного токена в
  `validate-blueprint-compliance.mjs`, LOC-исключения для прозы в `pre-edit.mjs`,
  протокол локального Spark через SSH
- **PLPilot** — секция 3.6 `review.md`: мутационное доказательство для security/money
  инвариантов, завязанное на его `scripts/fitness/` и `scripts/mutation/`
- **Dowry** — `seed-lessons`, расширенный на FTR/TECH/ARCH (файл сам это документирует)
- **Dowry-mc** — автопилот со свёрнутым внутрь spec-reviewer
- **gipotenuza** — `coder.md`, указывающий на `.claude/rules/domains/{domain}.md`

**Проверено измерением, а не рассуждением.** Связность промптового дерева после
обновления улучшилась: Dowry 63 → 56 находок `check-prompt-integrity`, wb 21 → 16,
gipotenuza 12 против 21 у ещё не синканного соседа.

### Два дефекта, вскрытых по дороге

1. **`template/scripts/` не содержал двух скриптов, которые его же промпты зовут** —
   `pre-review-check.py` и `build-lessons-index.py`. При этом `dependencies.md`
   утверждает «Each ships in `template/scripts/` too». Скопированы.
2. **Обратное течение опыта.** Гейт показал у AwardyBot `differs=43` против baseline 42
   — и за этой единицей стоял не мусор, а исправление: три условия `not suffix == ".py"`
   его линтер поправил на `suffix != ".py"` (ruff SIM201), к нам это не вернулось.
   `ruff check --select SIM` на копии DLD давал три ошибки. Версия принята наверх.

   Это единственный механизм обратного потока, который у нас теперь есть. Раньше опыт
   из проектов возвращался в фреймворк только через человека, случайно заметившего.

---

## Часть C — второй проход: что мешало обновиться

Первый проход оставил 85 файлов. Оказалось, часть из них держали не специфику, а
**два механизма заморозки** — и оба были видны только потому, что дрейф стал измеримым.

**1. Собственная точечная правка.** Утренняя вставка строки `GROWTH` в `autopilot-git.md`
и `worktree-setup.md` сделала эти 8 файлов «локально изменёнными» — и безопасное
обновление их пропустило. Классическая ловушка: аккуратная ручная правка стоит дороже,
чем кажется. Снял вставку, GROWTH теперь приезжает из самого template.

**2. Одна строка frontmatter замораживала весь файл.** Проект имеет право настраивать
`model:` / `effort:` — это его счёт за роутинг. Но изменённая шапка делала файл не
совпадающим ни с одной версией template, и **проза под ней оставалась месяцами старой**.
Так замёрзли восемь агентов AwardyBot: `model: haiku → sonnet` удерживал bug-hunt промпты
на ревизии, которая была *до* правил анти-галлюцинации и поля `confidence`.

Теперь тело и frontmatter сравниваются раздельно: тело обновляется, настройка проекта
сохраняется дословно. Ещё шесть bug-hunt агентов AwardyBot оказались тем же случаем —
их единственной «правкой» был `@output-conventions.md`, который **в template уже есть**.

### Итог по флоту

| Проект | было | после части B | после C | **после D** |
|---|---:|---:|---:|---:|
| AwardyBot | 92 | 42 | 35 | **27** |
| Dowry | 94 | 11 | 10 | **10** |
| Dowry-mc | 99 | 12 | 11 | **11** |
| gipotenuza | 94 | 7 | 6 | **6** |
| PLPilot | 94 | 6 | 5 | **5** |
| wb | 95 | 5 | 4 | **4** |
| MishkinLyap | 91 | 2 | 0 | **0** ← |

**MishkinLyap полностью синхронизирован** и получил первый штампованный
`.claude/DLD_VERSION`. Это и есть то, как выглядит конечное состояние: маркер пишется
только когда расхождений действительно ноль, и перепроверяется с диска при каждом запуске.

Остаток — проектная специфика, перечисленная в `fleet-drift-baseline.txt` поимённо.

### Обратный поток, второй случай

Правило AwardyBot «при смене сигнатуры — отдельный `grep` по `tests/`, и **каждый**
caller-тест в Allowed Files» поднято в оба дерева DLD. Цена этого правила известна
поимённо: TECH-1325 назвал 2 тест-файла, сломалось 5, автопилот расширял себе scope на
ходу — семь ретраев. В template такого правила не было вовсе.

---

## Часть D — роутинг решает фреймворк, а не проект

Часть C оставила у AwardyBot восемь файлов, расходящихся с template **только** строкой
`model:`/`effort:`, и объяснила это тем, что настройка роутинга — счёт проекта, который
он платит сам. Такого разрешения никто не давал. Проверка, откуда эти шапки взялись,
показала, что и объяснение было выдумано задним числом:

| Шапка | Откуда | Что делала |
|---|---|---|
| 5 файлов | дословная шапка template от **февраля-марта 2026** | ничьё решение, просто снимок, который никто не обновлял |
| `audit/`, `board/`, `triz/synthesizer` | `model: opus`, чего в истории template не было **никогда** | merge-и-формат на самой дорогой модели |

Первые пять — обычная протухшая копия, которую старый контракт называл «настройкой
проекта» и берёг от обновления. Три оставшихся — ручная правка, которая три месяца
проходила мимо ревью, потому что в отчёте гейта была неотличима от строки в списке из
35 путей.

**Что изменено**

1. **Шапка — тоже контент фреймворка.** `--apply-clean` теперь сверяет её с историей
   шапок template: совпала с какой-либо прошлой версией → файл обновляется целиком;
   не совпала ни с одной → тело всё равно обновляется, а строка остаётся человеку.
   Гарантия «трогаем только то, что доказуемо ничьё» не нарушена ни в одну сторону.
2. **`OVERRIDE` — отдельный класс отказа, который baseline не покрывает.** Гейт называет
   файл и обе величины (`model: opus -> sonnet`) и возвращает 1, каким бы щедрым ни было
   число в реестре. Долг по коду проект копит осознанно; роутинг — нет.
3. **Правило записано в обоих деревьях.** В корневом `model-capabilities.md` — со ссылкой
   на гейт, в template — как урок словами: локальная правка шапки замораживает прозу под
   ней, нужен другой роутинг — поднимай в template, поедет весь флот.

**Побочно, в AwardyBot:** `.claude/scripts/lib/capture-artifacts.mjs` невозможно было
закоммитить — голый `lib/` из python-шаблона `.gitignore` ловит любой вложенный каталог
с таким именем. Его импортирует `.claude/scripts/run-eval.mjs`, то есть `/eval` там падал
бы на импорте. Заодно оказалось, что новые файлы в `miniapp/src/lib/` тем же правилом
молча не попадали в индекс (13 старых живут только потому, что были добавлены раньше).
Правка: `lib/` → `/lib/`.

**Проверка:** `python scripts/check-fleet-drift.py --root D:/dev` — 0 овверайдов на всех
семи проектах; `--apply-clean` по флоту: 0 изменённых файлов, протухших шапок больше нет.

---

## Что осталось

1. **Остаточная специфика — 71 файл** на семь проектов, перечислена в
   `scripts/fleet-drift-baseline.txt`. Это не долг: гейт краснеет, если проект уедет
   **дальше** этих чисел.
2. **`facilitator.md` у AwardyBot и Dowry** зовёт скаутов `spark-external` / `spark-patterns`,
   которых в template нет. Их `feature-mode.md` уже переведён на трёх скаутов, а фасилитатор
   остался на старой схеме — развязать это отдельно.
3. **Строчные цитаты в `.claude/rules/dependencies.md`** (`run-agent.sh:47`, `:50`, `:51`) —
   настоящие ссылки на живой код, протухнут так же. Стоит спеки.
4. **Обратный поток опыта** работает как побочный эффект гейта дрейфа (так нашлись ruff
   SIM201 и правило про caller-тесты). Отдельного механизма «поднять находку из проекта
   во фреймворк» по-прежнему нет.

---

## Файлы

| Что | Где |
|---|---|
| Сторож дрейфа | `scripts/check-fleet-drift.py` |
| Реестр специфики | `scripts/fleet-drift-baseline.txt` |
| Тесты сторожа (16) | `tests/unit/test_check_fleet_drift.py` |
| Сторож цитат | `scripts/vps/tests/test_doc_symbol_refs.py` |
| Parity префиксов (+ даунстримы через `FLEET_ROOT`) | `scripts/vps/tests/test_branch_prefix_parity.py` |

```bash
python scripts/check-fleet-drift.py --root D:/dev                  # где флот сегодня
python scripts/check-fleet-drift.py D:/dev/<проект> --apply-clean  # безопасное обновление
python scripts/check-fleet-drift.py D:/dev/<проект> --apply        # только когда differs=0
```
