# Автопилот на оркестраторе: почему 2–3 часа и $15–50 за спеку

Разбор по данным VPS (`task_log`, per-run JSON-логи, транскрипты сессий Claude Code за
07.08–02.09) и по истории промптов в этом репо. Вопрос: почему спека, которую в интерактиве
делают за 10–30 минут, на оркестраторе живёт 2–3 часа и упирается даже в поднятый до 3 часов
таймаут, и что изменилось с мая–июня.

## Вывод в трёх строках

1. **Половина времени прогона — не модель, а `pytest`.** Один прогон `tests/architecture/`
   awardybot — 5–9 минут на любой машине (VPS пустой 333 с, твой ноутбук 517 с), а конвейер
   запускает тяжёлые наборы 5–6 раз на спеку: baseline в PHASE 0, тестер по 2–3 раза на каждой
   задаче, кодер сам, гейты main loop, `./test ci` дважды в PHASE 3. `steal` 30–70 % на VPS
   в рабочие часы добавляет сверху проценты, не разы.
2. **Вторая половина — конвейер из 12–14 холодных агентов на спеку**, каждый стартует с
   28–46K токенов системного промпта и разгоняется до 160–390K, плюс main loop на Opus, который
   с 25–27.07 делает review, spec-compliance и гейты сам: 220 сообщений и 108 Bash-вызовов на
   одну спеку. Деньги уходят в cache read: 27M токенов на Opus и 42M на Sonnet за один прогон.
3. **Май «быстрым» был наполовину фиктивно.** Медиана 2 минуты — это петли re-dispatch и
   ранние выходы «уже сделано / blocked». Реальные майские прогоны (≥25 ходов) стоили $7.4 и
   шли 20–50 минут. Сейчас медиана $15–17 и 124 минуты, то есть честное замедление ×3 и
   удорожание ×2, а не ×10.

## Цифры

Успешные автопилот-прогоны по `task_log`, минуты wall-clock:

| Месяц | n | p50 | p75 | p90 | max | доля ≥60 мин |
|-------|---|-----|-----|-----|-----|--------------|
| 2026-05 | 557 | 2 | 21 | 45 | 154 | 6 % |
| 2026-06 | 188 | 24 | 37 | 54 | 201 | 7 % |
| 2026-07 | 83 | 30 | 44 | 80 | 386 | 16 % |
| 2026-08 | 82 | 53 | 80 | 93 | 159 | 45 % |
| 2026-09 | 12 | 124 | 134 | 166 | 168 | 75 % |

Медианы по JSON-логам прогонов (только `skill=autopilot`):

| Период | n | ходов main loop | output-токенов main loop | cache read main loop | стоимость |
|--------|---|-----------------|--------------------------|----------------------|-----------|
| май–июнь | 800 | 10 | 2.9K | 0.5M | $1.47 |
| май–июнь, только ≥25 ходов | 254 | 51 | — | — | $7.44 |
| 26.07–22.08 | 98 | 68 | 8.7K | 2.1M | $10.37 |
| с 23.08 | 62 | 70 | 27.4K | 5.75M | $16.59 |

Расход за 23.08–02.09 по логам: автопилот 80 прогонов, **$1 187**, медиана $14.9, максимум
$52.9. Плюс `/qa` после каждой спеки ($159 за 34 прогона, 17 мин в среднем) и `/reflect`
($93, 14 мин). В мае `/qa` шёл 5 минут, `/reflect` — 3.

## Куда уходит время: FTR-1487 (awardybot, 166 мин, $40.83)

По транскрипту главной сессии и её 12 субагентов, минуты:

| Что | Минут | Доля |
|-----|-------|------|
| Baseline `./test fast` в PHASE 0 (две попытки: 5 + 25) | 30 | 18 % |
| Planner (Opus, effort high, 94 сообщения, 357K контекста) | 12 | 8 % |
| Tester ×2 (Sonnet), из них Bash = pytest | 45 / 41 | 27 % |
| Coder ×9 (Sonnet), из них Bash = pytest и гейты | 44 / 17 | 27 % |
| Main loop, ожидание модели (Opus, 223 сообщения) | 22 | 14 % |
| Main loop, свои Bash-гейты после планировщика (108 вызовов) | 14 | 9 % |

Итого `pytest` и гейты — **~88 из 166 минут**. Один прогон `tests/architecture/` на VPS
занимал 4.7 и 7.2 минуты; тестер запускал его дважды за задачу, потом ещё три раза целевые
подмножества по 1.5–2 минуты. Второй тестер (Task 2) — 30 Bash-вызовов, 22 минуты чистого pytest.

Деньги того же прогона: Opus $22.7 (cache read 27M токенов = $13.5, cache write 655K = $6.5,
output 107K = $2.7), Sonnet $18.1 (cache read 41.8M, cache write 2.0M, output 167K). Thinking-токены
у обеих моделей — 26K и 70K, то есть **«модель много думает» здесь ни при чём**: счёт растёт от
числа сообщений, умноженного на размер контекста.

Та же раскладка по всем длинным прогонам с 24.08 (минуты; `testerBash` — pytest внутри тестера):

| Спека | всего | baseline | main Bash | main LLM | planner | coder | tester | testerBash | coderBash |
|-------|------:|---------:|----------:|---------:|--------:|------:|-------:|-----------:|----------:|
| awardybot:FTR-1465 | 144 | 9 | 28 | 41 | 13 | 37 | 37 | 30 | 15 |
| dowry:BUG-479 (timeout) | 180 | 3 | 1 | 17 | 16 | 30 | 85 | 74 | 14 |
| awardybot:FTR-1467 (timeout) | 180 | 8 | 37 | 21 | 11 | 30 | 82 | 75 | 18 |
| awardybot:TECH-1471 (timeout) | 160 | 9 | 23 | 20 | 10 | 29 | 39 | 36 | 14 |
| dowry-mc:BUG-421 | 159 | 3 | 57 | 29 | 14 | 44 | 18 | 19 | 33 |
| dld:TECH-220 | 115 | 1 | 27 | 20 | 12 | 36 | 23 | 22 | 19 |
| dowry:TECH-491 | 167 | 3 | 14 | 24 | 16 | 68 | 52 | 49 | 53 |
| awardybot:FTR-1485 (timeout) | 180 | 7 | 19 | 21 | 7 | 76 | 36 | 34 | 46 |
| awardybot:FTR-1487 | 165 | 31 | 13 | 22 | 12 | 44 | 45 | 41 | 17 |
| awardybot:TECH-1480 | 123 | 6 | 60 | 17 | 9 | 23 | 0 | 0 | 93 |
| awardybot:TECH-1486 | 125 | 9 | 32 | 23 | 9 | 61 | 0 | 0 | 27 |

Закономерность одна и та же: тестер — это почти целиком pytest; у кодера от трети до
половины времени — тоже pytest и гейты; planner — стабильные 10–16 минут на каждой спеке;
main loop на Opus — 20–40 минут ожидания модели плюс до часа собственных Bash-гейтов
(TECH-1480: 60 минут, BUG-421: 57).

## VPS и `steal`: множитель, но не главный

`sar` за 28.08 и 02.09, почасовые средние:

| Час (28.08) | steal | idle | Час (02.09) | steal | idle |
|-------------|------:|-----:|-------------|------:|-----:|
| 01:00 | 65 % | 11 % | 09:00 | 70 % | 6 % |
| 11:00 | 56 % | 22 % | 13:00 | 69 % | 6 % |
| 13:00 | 69 % | 6 % | 14:00 | 62 % | 14 % |
| 15:00 | 70 % | 5 % | 16:00 | 46 % | 35 % |
| 23:00 | 66 % | 8 % | ночь | 6 % | 88 % |

Среднесуточный `steal` 27–30.08 — 30–42 %. Это не наша нагрузка, это сосед по гипервизору:
как только мы грузим CPU, хостер забирает от трети до двух третей тактов. На 6 vCPU при этом
идут 2–3 параллельных прогона, каждый с `pytest -n 4` (addopts в `pyproject.toml` awardybot),
то есть 8–12 воркеров на 2–4 реально доступных ядра.

Сравнение одного и того же набора `tests/architecture/` (awardybot, `-p no:cacheprovider`):

| Где | Секунд |
|-----|-------:|
| VPS под нагрузкой (внутри тестера FTR-1487) | 282 и 432 |
| VPS ночью, пустой (load 0.99, steal 0 %) | 333 (1813 passed) |
| Машина Олега (Windows, тот же коммит) | 517 (1722 passed, 94 failed — Windows-специфика) |

Замер 02.09 23:14 местного. Вывод неожиданный и важный: **набор сам по себе занимает 5–9 минут
на любой машине**, а под нагрузкой на VPS — 4.7–7.2. `steal` добавляет проценты, не разы.
Значит, главный множитель — не хостер, а **сколько раз конвейер запускает этот набор**: в
FTR-1487 тестер гонял `tests/architecture/` дважды на задаче, плюс baseline `./test fast`
(куда он входит) в PHASE 0, плюс `./test ci` дважды в PHASE 3. Пять–шесть прогонов по
5–7 минут — вот откуда 40–90 минут pytest на спеку.

## Почему интерактив быстрее — по пунктам

| Автопилот на VPS | Интерактив на ноутбуке |
|------------------|------------------------|
| PHASE 0: `./test fast` целиком до начала работы (5–30 мин) | не делается |
| PHASE 1: planner на Opus всегда, даже если план в спеке есть (10–16 мин) | план держится в голове сессии |
| На каждую задачу холодный coder + холодный tester: старт 28–35K токенов, обязательное чтение `architecture.md` + `dependencies.md` + `domains/*.md` (в awardybot это 6.2K + 3.4K + 19.7K слов), в конце `context-updater` пишет обратно в domain-файлы | один контекст, читается только то, что нужно |
| Тестер гоняет позиционные наборы + `tests/architecture/` по 2–3 раза на задачу | целевой тест один раз, полный набор — один раз в конце |
| Main loop после каждой задачи: spec compliance, review по чеклисту `review.md`, ratchet, size gates, arch gates — 10 Bash-вызовов по 1 минуте (все на VPS с steal) | коммит |
| PHASE 3: `./test ci` на ветке, потом ещё раз на смерженном дереве, Exa-верификация, reflect, documenter | push |
| После done: отдельный `/qa` (17 мин) и `/reflect` (14 мин) | нет |
| 2–3 прогона делят 6 vCPU с 30–70 % steal | вся машина твоя |

Дело не в окне контекста и не в «модель думает дольше»: thinking-токены в прогоне —
единицы процентов расхода, а задержка модели в main loop — медиана 4 секунды на сообщение.
Дело в **количестве сообщений и запусков pytest**, которое сам конвейер и назначает.

## Что изменилось с мая–июня (хронология)

| Дата | Коммит | Что сделал | Эффект на время/деньги |
|------|--------|-----------|------------------------|
| до 26.07 | — | pueue запускал замороженный CLI 2.1.72 → под именем `claude-opus-5` фактически шёл `opus-4-6` и `sonnet-4-6` на 200K с автокомпактом каждые ~155K | искусственный «регулятор»: контекст обрезался, агенты вынужденно сходились |
| 25.07 | `262d4f5` | rebalance агентов и скиллов под Opus 5 / Sonnet 5 | — |
| 26.07 | `a710cf5` | CLI резолвится по версии → реальные Opus 5 / Sonnet 5 | ходы main loop 11 → 49 медиана, стоимость $2.6 → $19.5 (замер в `6cb1bf5`) |
| 27.07 | `77d5b09` | spec-compliance проверяется в main loop, без субагента | +ходы Opus |
| 27.07 | `ba33aeb` | documenter подключён в PHASE 3 | +1 субагент на спеку |
| 24.08 | `69d1580` | code-quality review тоже в main loop (`review.md` как чеклист) | main loop сам гоняет bash-проверки из review.md — те самые 108 Bash-вызовов |
| 23.08 | `6cb1bf5` | TIMEOUT 90 → 180 мин, MAX_TURNS 120 → 300 | прогоны перестали умирать на 90-й минуте и стали жить до 180 |
| 30.08 | `2497f61` | Bash-таймаут CLI 120 с → 15 мин (макс 30) | 25-минутный `./test fast` теперь легально живёт внутри одного вызова; до этого суиты дольше 2 минут убивались |
| май → авг | — | коммитов на спеку: awardybot 4.3 → 7.1, dowry 3.4 → 12.1 | спеки стали крупнее в 1.6–3.5 раза |
| авг | хостер | `steal` 30–70 % в рабочие часы | pytest в 2–3 раза медленнее, чем на пустой машине |

Каждый шаг по отдельности логичен и задокументирован. Вместе они дали конвейер, в котором
на одну спеку приходится 12–14 холодных агентов, 4–6 полных прогонов тестов на медленной
машине и main loop на Opus, который сам стал ревьюером.

## Что с этим делать (рычаги по убыванию эффекта)

1. **Тесты: один целевой прогон на задачу, один полный — на спеку.** Убрать baseline
   `./test fast` из PHASE 0 (CI на `origin/develop` уже проверяется `ci-status.sh`), запретить
   тестеру `tests/architecture/` и полные наборы на задаче — они идут один раз в PHASE 3.
   Ожидаемый эффект: −40…−70 минут на спеку, −30 % стоимости Sonnet.
2. **Main loop не гоняет гейты после каждой задачи.** Ratchet, size gates, arch gates — один раз
   перед коммитом задачи, не по 10 вызовов. Либо вернуть review в один субагент на Sonnet, но
   с явным запретом на запуск pytest. Эффект: −15…−50 минут, −25 % стоимости Opus.
3. **Planner условный.** Если спека моложе N дней и `Drift Log` пуст — планировщик проверяет
   пути `Glob`-ом и выходит, а не перечитывает 350K токенов кода на Opus high. Эффект: −10 минут
   и −$3…5 на каждой спеке.
4. **Холодный старт субагентов дешевле.** `context-loader` заставляет каждый coder читать
   `architecture.md` + `dependencies.md` + все затронутые `domains/*.md` (в awardybot ~30K слов),
   `context-updater` — писать обратно. Сделать чтение по требованию (grep по имени сущности),
   а обновление контекста — одной задачей в PHASE 3. Эффект: −20…−30 % cache write у Sonnet.
5. **Сам набор `tests/architecture/` — 1813 тестов за 5,5–8,4 минуты на пустой машине.**
   Профиль `--durations=25` (03.09, пустой VPS, 504 с): один тест
   `test_ci_integration_has_at_least_one_test` — **105 с**, три «real flows» (`ff_delivery`,
   `ff_payment`, `ff_search_image`) — по 26–31 с, `test_all_flow_refs_resolve` — 18 с; пять
   тестов = 210 с из 504. Остальные 1800 — секунды. Плюс в «архитектурной» папке живут
   поведенческие классы (`TestRiskLabelsBehaviour`, `TestPayoutBehaviour`). Вынести пятёрку под
   маркер `slow_arch` и гонять её раз в PHASE 3; per-task набор станет 3–4 минуты. Хостер и
   `steal` — второстепенно: под нагрузкой 4.7–7.2 мин против 5.5–8.4 на пустой.
6. **`/qa` и `/reflect` — не после каждой спеки.** 31 минута и ~$8 на спеку. `/reflect` — раз в
   сутки по проекту, `/qa` — только для FTR/BUG с `## Acceptance Verification`.

Первые два пункта — правки промптов в `.claude/skills/autopilot/` и `.claude/agents/tester.md`
в обоих деревьях, без кода оркестратора. Мерить после — тем же скриптом, что писал этот разбор:
`/tmp/ap/batch.py <дата>` на VPS раскладывает каждый прогон по категориям из транскриптов.

## Главная находка 03.09: контекст агентов раздувают правила по `paths:`, а не код

Вопрос Олега: «планер жил на 200K и прекрасно работал, а теперь ему надо 400K?». Проверка по
транскрипту планера FTR-1487: всё, что он прочитал инструментами, — **45K токенов** (183K
символов: 26 Read, 14 Grep, 10 Glob). Контекст при этом вырос до 357K, и скачки не совпадают с
чтением: на 3-м сообщении **+88K** при результате инструмента в 2K, на 22-м +34K при 1K, на 37-м
+18K при нуле. У тестера — **+123K** после одного Read файла из `src/api/v2/`. В транскрипте
этих токенов нет.

Прямой замер на машине Олега, AwardyBot, `claude -p` с одним `Read src/domains/seller/…`:

| | контекст после Read | из них cache write |
|---|---:|---:|
| `.claude/rules/` на месте | **+67K** | 67K |
| `.claude/rules/` убрана | +7K | 7K |

Разница — 60K токенов на **одно** касание одного пути: Claude Code подшивает в контекст все
`rules/*.md`, чьи `paths:` совпали (`architecture.md` 6.2K слов на `src/**`, `notifications.md`,
`architectural-integrity.md`, `dependencies.md` на `src/domains/**`, `domains/seller.md`) — 15.5K
слов, по-русски это ~4 токена на слово. Агент, который за прогон трогает `src/api`, `src/domains`,
`tests`, `scripts/cron`, собирает практически все правила проекта:

| Проект | слов в `.claude/rules/` | ≈ токенов при полной подгрузке |
|--------|------------------------:|-------------------------------:|
| awardybot | 57 420 | ~220K |
| dowry | 23 064 | ~90K |
| plpilot | 16 756 | ~65K |
| dld | 14 239 | ~55K |
| dowry-mc | 9 710 | ~38K |

Это и есть ответ на «почему 400K»: планер не стал нуждаться в большем — у него отобрали
ограничитель (200K с автокомпактом резал именно этот балласт), а балласт вырос. И это же
объясняет деньги FTR-1487: cache write у Sonnet 2.0M токенов ≈ 12 субагентов × ~170K правил,
у Opus 655K ≈ main loop + планер. Каждый холодный агент платит за подшивку заново, а потом
каждое его сообщение читает её из кэша — cache read 27M у Opus и 42M у Sonnet за прогон.

Правило «`paths:` обязателен» (26.07) экономит токены в интерактиве, где сессия одна. В
автопилоте на спеку 12–14 агентов, и каждый трогает много путей — правила грузятся в каждого,
почти целиком. Механизм, придуманный ради экономии, стал главной статьёй расхода.

**Что с этим делать (переформулированный рычаг 4, теперь первый по деньгам):**
1. Диета правил в awardybot: 57K слов → ≤ 12K агентских инвариантов. Нарратив (`payments-changelog`,
   `payments-forensics`, `payments-attribution`, ранбук `crons.md`, история в `domains/*.md`) —
   в `docs/`, агенты читают по имени через grep, когда нужно. Это спека в awardybot.
2. Сузить глобы: `src/**` на `architecture.md` / `notifications.md` / `architectural-integrity.md`
   срабатывает на любой файл. Разбить `architecture.md`: анти-паттерны (≈1K слов) на `src/**`,
   остальное — по требованию. `tests/**` не должен тянуть доменные правила.
3. Во фреймворке: потолок на `.claude/rules/` (слов) с проверкой в `check-rules-loading.py` — та же
   логика, что «paths обязателен», но про массу, не про заголовок.
4. Замер после: тот же `claude -p` с одним Read — ожидаем +7…+15K вместо +67K; по прогонам —
   падение cache write на Sonnet в разы.

**Сделано 03.09 (продолжение):**
- AwardyBot `649260a` — диета: ADR-таблица → `docs/adr-index.md`, Change History пяти
  доменов и payments → `docs/rules-history-*.md`, ранбуки серверных кронов →
  `docs/runbook-crons-servers.md`, в правилах указатели; `architectural-integrity.md`
  переведён на `ai/features/**` + промпты агентов, у `dependencies.md` снят `tests/**`.
  Масса 57 420 → 43 580 слов. Тот же Read на пустом клоне VPS: **+67K → +38K токенов**.
  Половина остатка — `dependencies.md` (3 410 слов на любое касание `src/domains/**`) и
  `notifications.md` (2 212 на `src/**`); следующий шаг — вынести карту зависимостей из
  `.claude/rules/` в файл, который читают grep'ом (правка во фреймворке: `context-loader`,
  `review.md`, обе копии).
- dld `4b62cab` — `check-rules-loading.py` (оба дерева) считает массу scoped-правил, печатает
  топ-5 и краснеет выше `--max-words` (25 000). Флот на 03.09: AwardyBot 57K (до диеты) —
  красный, Dowry 23,8K — на грани плюс **`generated/schema.md` без `paths:` = 10,7K слов в
  каждую сессию**, plpilot 16,6K, Dowry-mc 9,8K, wb 9,2K, gipotenuza 3,5K, MishkinLyap 2,1K.

**Урок диеты, 04.09: правила читают не только агенты, но и фитнес-тесты.** Через час после
`649260a` CI awardybot покраснел: `test_clobber_guard::test_adr043_exists_in_architecture`
искал ADR-043 в `.claude/rules/architecture.md`, а `test_money_crons_outside_msk_rollover`
парсил таблицу серверных кронов из `.claude/rules/crons.md` — оба файла диета выпотрошила.
Починено в тестах (AwardyBot `2e8e8621f`: ADR-индекс читается из `docs/adr-index.md`,
расписания — из обоих файлов SSOT одним текстом). Перед следующим выносом из `.claude/rules/`
в любом проекте: `grep -rn "rules/<файл>" tests/` — в awardybot таких читателей 18 файлов,
большинство лишь ссылаются в докстрингах, но `test_campaign_fsm_invariants`,
`test_money_crons_*`, `test_clobber_guard` открывают файл и ищут в нём строки.

## Что сделано 03.09 — рычаги 1 и 2

Правки промптов, оба дерева (`.claude/` и `template/.claude/`), код оркестратора не тронут.
Гейты зелёные: `check-prompt-integrity` ×2, `check-tree-sync`, `check-rules-loading`,
пять harness-сьютов `test/scripts`, `pytest tests/unit` (196).

**Рычаг 1 — тесты один раз на задачу, полный набор один раз на спеку:**
- `worktree-setup.md` шаг 9 и `autopilot-git.md` §2.6: baseline `./test fast` в PHASE 0 убран,
  базой считается CI на `origin/develop`, который шаг 1 уже проверяет.
- `tester.md`: новая секция «Test Budget» — одна выборка, один прогон, после фикса перегон только
  упавших id, запрет на `./test fast|ci`, `tests/architecture/`, целые `tests/`/`src/`,
  бюджет 10 минут pytest на диспатч; fallback «нет совпадения → полный набор» заменён на
  «свой тест-файл, иначе `no_tests_selected`».
- `coder.md` шаг 6: кодер гоняет только тесты своей задачи, один раз, после фикса — упавшие id.
- `task-loop.md` Step 2: тестеру передаётся `pytest {Test: файлы задачи}`, а не `./test fast`.
- `finishing.md` + `autopilot-git.md` §5.1/§5.4: `./test ci` — единственный полный прогон; шаг 8
  сравнивает `git rev-parse HEAD^{tree}` с деревом, проверенным на шаге 1, и при совпадении
  логирует `CI_PARITY_REUSED` вместо второго 5–25-минутного прогона.

**Рычаг 2 — main loop не гоняет гейты после каждой задачи:**
- `task-loop.md`: секция «What the loop never runs» — main loop не запускает pytest, `./test *`,
  `tests/architecture/` и проектные гейты, их обёртывающие; Step 3/5 — только `grep`/`wc`/
  названные скрипты, один прогон на задачу, после возврата от кодера перегоняется только
  упавшая проверка.
- `review.md` §0: `cat dependencies.md` (3.4K слов в контекст на каждой задаче) → `grep -n`
  по изменённым модулям.

**Чего это не делает само по себе.** Оркестратор на VPS исполняет `.claude/` каждого проекта,
а не dld. Пока файлы не раскатаны на awardybot/dowry/dowry-mc/plpilot (`scripts/check-fleet-drift.py
--apply-clean`, как 31.08), прогоны идут по старым промптам. Раскатка — следующий шаг, после
решения по рычагам 3–6. Проверено сухим прогоном тех же замен по семи локальным чекаутам:
MishkinLyap принимает все 8 файлов как есть; у AwardyBot, Dowry, Dowry-mc, PLPilot, wb и
gipotenuza 6 файлов из 8 патчатся один-в-один, а `autopilot-git.md` и `finishing.md` держат
редакцию до 21.06 (финальный тест там до сих пор `./test fast`, CI-parity гейт TECH-206 до
флота не доехал) — для них снипеты подобраны под старый текст.

**Раскатано 03.09** свежими клонами на VPS, `fleet_apply.py`, prompt-integrity до/после
без новых findings: AwardyBot `cb3b953`, Dowry `d6bd00a`, Dowry-mc `2f160b7`, plpilot `1706efe`,
wb `0801dec`, gipotenuza `98bc5bd`, MishkinLyap `82a4c91` — все в `develop`. Оркестратор
подтягивает `origin/develop` на каждом цикле; следующие диспатчи идут по новым промптам.

## Что проверить нельзя

- Транскриптов за май нет — ретенция Claude Code начинается с 07.08. Майская картина только
  по `task_log` и JSON-логам прогонов, без раскладки на тесты/агентов.
- `sar` хранит 28 дней: был ли такой `steal` в мае — неизвестно.

## 04.09 — волна 1–3 сентября: она шла на старых промптах, и это худший срез за всё время

Вопрос Олега: волна спек 1–3 сентября уже отработала на свежих правилах — как это поменяло
игру относительно июля и августа? Ответ по данным: **не отработала.** Раскатка рычагов 1–2 на
флот легла в `develop` проектов 03.09 в 23:17 МСК (AwardyBot `cb3b953`, Dowry `d6bd00a`,
Dowry-mc `2f160b7`, plpilot `1706efe`), диета правил awardybot — в 23:29. Последний прогон
волны (awardybot BUG-1499) стартовал в 22:38 МСК. Worktree создаётся при диспатче, и в
`.worktrees/BUG-1493|1497|1499` лежит старый `tester.md` без секции «Test Budget». Первые
прогоны на новых промптах — три задачи, взятые 04.09 в 09:55–10:05 МСК (TECH-1481, 1491, 1501);
на момент записи они идут 110–120 минут и не закончены.

### Помесячно, по JSON-логам прогонов (`scripts/vps/logs/*.log` + heartbeat)

Только `skill=autopilot`; минуты и деньги — по успешным (`exit 0`); таймаут = `exit 124`.

| Срез | n | ok | таймаут | fail | p50 мин | p75 | p90 | $ p50 | $ p75 | $ сумма | ходов p50 | cache read p50 |
|------|--:|---:|--------:|-----:|--------:|----:|----:|------:|------:|--------:|----------:|---------------:|
| июнь | 1095 | 1006 | 12 | 77 | 23 | 35 | 48 | 2.1 | 7.6 | 4 590 | 13 | 0.6M |
| 1–25.07 (CLI 2.1.72, фактически opus-4-6) | 110 | 66 | 4 | 40 | 25 | 40 | 53 | 9.0 | 14.9 | 721 | 8 | 3.0M |
| 26–31.07 (реальный Opus 5) | 24 | 18 | 6 | 0 | 38 | 66 | 86 | 13.0 | 21.8 | 214 | 41 | 3.5M |
| 1–22.08 | 74 | 49 | 24 | 1 | 50 | 65 | 80 | 21.8 | 31.7 | 1 019 | 48 | 5.9M |
| 23–31.08 (таймаут 180) | 44 | 33 | 6 | 5 | 75 | 93 | 138 | 18.4 | 24.2 | 624 | 63 | 7.4M |
| **1–3.09** | 45 | 29 | 4 | 12 | **95** | 126 | 159 | **27.1** | 34.7 | 806 | 78 | 12.5M |

Три дня сентября против июля: медиана времени ×2.5–3.8, медиана денег ×2–3, ходов main loop
×2 против конца июля и ×10 против начала. Cache read на прогон — 12.5M токенов против 3M: это
и есть «правила по `paths:` × 12 холодных агентов», замеренное 03.09.

Двенадцать `fail` — это в основном не работа: 9 прогонов по 1 ходу и 4 минуты (dowry BUG-502,
TECH-499, TECH-500 по два-три раза, awardybot BUG-1495) — диспатч упал до старта и оркестратор
переставлял их в очередь; плюс ARCH-487/FTR-488/FTR-489 по 0 минут 01.09.

**Таймауты в сумму денег не входят.** У прогона с `exit 124` в логе `cost_usd: 0.0`. По
транскриптам: FTR-1485 (первая попытка) — 136M cache read + 5M cache write + 330K output,
BUG-1493 — 80–100M cache read; у FTR-1487, обошедшегося в $40.8, было 69M. То есть каждый
таймаут стоил порядка **$60–90 и записал ноль**. Четыре таймаута волны — все awardybot
(FTR-1485, BUG-1493, BUG-1497, BUG-1499) — это ещё ~$300 сверх $806. Всего за три дня с `/qa`
($108) и `/reflect` ($66): **~$1 300 на 29 закрытых спек, ~$45 за спеку «под ключ».**

По проектам (ok-прогоны): awardybot 1–22.08 медиана 50 мин / $22.8 → 1–3.09 **118 мин / $25.2**;
dowry 23–31.08 49 мин / $23.0 → **93 мин / $29.5**. Спеки при этом не выросли: коммитов на
спеку в awardybot июль 5 → август 8 → сентябрь 6, в dowry 5 → 12 → 11. Сентябрьские спеки
это в основном BUG-и из sentry-bughunt 02.09 — по массе они как августовские.

### Раскладка волны по транскриптам (минуты; старые промпты)

| Спека | всего | main Bash | main LLM | planner | coder | tester | testerBash | coderBash |
|-------|------:|----------:|---------:|--------:|------:|-------:|-----------:|----------:|
| dowry:FTR-488 | 98 | 26 | 28 | 14 | 14 | 26 | 23 | 6 |
| dowry:TECH-491 | 167 | 14 | 24 | 16 | 68 | 52 | 49 | 53 |
| awardybot:BUG-1488 | 133 | 10 | 13 | 12 | 89 | 0 | 0 | 34 |
| awardybot:TECH-1479 | 124 | 30 | 20 | 13 | 63 | 0 | 0 | 41 |
| awardybot:TECH-1480 | 123 | 60 | 17 | 9 | 23 | 0 | 0 | 93 |
| dowry:BUG-493 | 98 | 18 | 37 | 12 | 62 | 17 | 14 | 33 |
| dowry:BUG-496 | 96 | 6 | 14 | 12 | 64 | 0 | 0 | 28 |
| awardybot:BUG-1490 | 96 | 42 | 12 | 7 | 31 | 0 | 0 | 9 |
| awardybot:BUG-1492 | 129 | 89 | 17 | 10 | 63 | 0 | 0 | 63 |
| dowry:BUG-502 | 172 | 12 | 24 | 20 | 68 | 45 | 36 | 26 |
| awardybot:BUG-1495 | 136 | 17 | 17 | 8 | 76 | 15 | 11 | 41 |
| awardybot:BUG-1497 (t/o) | 189 | 80 | 26 | 9 | 55 | 11 | 8 | 53 |
| awardybot:BUG-1499 (t/o) | 190 | 51 | 14 | 13 | 66 | 31 | 27 | 27 |

Заметная перемена против конца августа: **тестер исчез из половины прогонов** (0 минут у
BUG-1488, TECH-1479/1480, BUG-1490/1492/1496), а его работа переехала в кодера (`coderBash`
34–93 минуты) и в main loop (`main Bash` 42–89 минут у BUG-1490/1492/1497). Время не ушло, оно
переместилось. Именно эти два места рычаги 1–2 и запрещают — но волна их не видела.

### Первые три прогона на новых промптах (04.09, живой снимок на 110–120-й минуте)

| Спека | минут | main Bash | main LLM | planner | coder | tester | что видно |
|-------|------:|----------:|---------:|--------:|------:|-------:|-----------|
| TECH-1481 | 89+ | **4.7** | 13.6 | 11.3 | 46.4 (6 вызовов) | 17.1 (4 × 3–5 мин) | рычаги 1–2 соблюдены; тестер гоняет один файл на задачу |
| TECH-1491 | 92+ | **24.3** | 14.4 | 6.3 | 44.6 (8) | 6.8 (2) | main loop сам запустил `./test fast` (5 мин), `pytest tests/ -k invoice` дважды по 6 мин и в конце `pytest tests/ src/ -n 4` с таймаутом 1700 с |
| TECH-1501 | 106+ | 6.9 | 20.2 | 8.7 | 37.9 (3) | 39.0 (3) | тестер Task 5 — 29.5 минут pytest в 16 вызовах при бюджете 10 минут |

Что это говорит:

1. **Там, где промпт послушали, эффект есть и большой.** Main Bash 4.7–6.9 минуты против
   42–89 в волне; тестер 3–5 минут на задачу против 20–40. У TECH-1481 pytest в main loop нет
   вообще.
2. **Но в двух прогонах из трёх Opus нарушил новые правила текстом по тексту.** TECH-1491 —
   ровно то, что «What the loop never runs» запрещает; TECH-1501 — тестер втрое превысил бюджет.
   Промпт — совет, не закон. Ограничение, которое должно держаться, надо ставить хуком
   (`PreToolUse` на `./test fast|ci`, `pytest tests/` без пути к файлу) и таймаутом в
   `test-wrapper`, а не абзацем.
3. **Даже у послушного TECH-1481 — 89 минут и не конец.** Пять задач × (кодер 7–11 мин +
   тестер 3–5 + Opus между ними 2–3) ≈ 15 минут на задачу — это пол конвейера при текущей
   архитектуре «холодный агент на каждую задачу». Рычаги 1–2 срезают верх (таймауты, лишний
   pytest), но не приводят к 30 минутам. К 30 минутам ведёт только меньше холодных стартов:
   один кодер на всю спеку или задачи крупнее.
4. **Мелочь, которая стоит по 3–5 минут на субагента:** тестер и кодер не находят `uv`/`pytest`
   (`which pytest`, `cat test-wrapper.mjs`, `ls .venv`) — `$HOME/.local/bin` не в PATH процесса.
   В 9 прогонах из 36 сентябрьских, 26 вызовов; в трёх живых — у пяти субагентов из
   двадцати. Чинится одной строкой в `run-agent.sh`.

### Что делать с «руки тянутся допилить»

Не трогать промпты до выборки. Три прогона — не замер, а первое впечатление; п. 1 в разделе
«Два правила для любого будущего замера» относится и сюда. Нужно 10+ прогонов на новых
промптах (при текущем темпе — сутки-двое), затем тот же `batch.py`. До этого имеет смысл
только то, что не спорит с замером:

- PATH для субагентов в `run-agent.sh` — убирает 3–5 минут возни на агента, ничего не меняет
  в логике.
- Хук вместо абзаца на «main loop не гоняет pytest» и `timeout` в `test-wrapper` — переводит
  рычаг 2 из совета в закон; иначе половина прогонов будет его игнорировать, как TECH-1491.
- Таймаут 180 → обратно 120 с обязательным salvage: четыре таймаута волны — треть денег трёх
  дней, и после 120-й минуты ни один из них не превратился в `done`.
